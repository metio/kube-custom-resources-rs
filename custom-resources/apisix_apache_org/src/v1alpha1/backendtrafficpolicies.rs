// WARNING: generated by kopium - manual changes will be overwritten
// kopium command: kopium --docs --derive=Default --derive=PartialEq --smart-derive-elision --filename crd-catalog/apache/apisix-ingress-controller/apisix.apache.org/v1alpha1/backendtrafficpolicies.yaml
// kopium version: 0.23.0

#[allow(unused_imports)]
mod prelude {
    pub use kube::CustomResource;
    pub use serde::{Serialize, Deserialize};
    pub use k8s_openapi::apimachinery::pkg::apis::meta::v1::Condition;
}

use self::prelude::*;

/// BackendTrafficPolicySpec defines traffic handling policies applied to backend services,
/// such as load balancing strategy, connection settings, and failover behavior.
#[derive(CustomResource, Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
#[kube(group = "apisix.apache.org", version = "v1alpha1", kind = "BackendTrafficPolicy", plural = "backendtrafficpolicies")]
#[kube(namespaced)]
#[kube(status = "BackendTrafficPolicyStatus")]
#[kube(schema = "disabled")]
#[kube(derive="Default")]
#[kube(derive="PartialEq")]
pub struct BackendTrafficPolicySpec {
    /// HealthCheck defines active and passive health check configuration for
    /// the upstream backends. When configured, APISIX will probe backends
    /// (active) or monitor live traffic (passive) to detect and bypass
    /// unhealthy nodes.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "healthCheck")]
    pub health_check: Option<BackendTrafficPolicyHealthCheck>,
    /// LoadBalancer represents the load balancer configuration for Kubernetes Service.
    /// The default strategy is round robin.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub loadbalancer: Option<BackendTrafficPolicyLoadbalancer>,
    /// PassHost configures how the host header should be determined when a
    /// request is forwarded to the upstream.
    /// Default is `pass`. Can be `pass`, `node` or `rewrite`:
    /// * `pass`: preserve the original Host header
    /// * `node`: use the upstream node’s host
    /// * `rewrite`: set to a custom host via `upstreamHost`
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "passHost")]
    pub pass_host: Option<BackendTrafficPolicyPassHost>,
    /// Retries specify the number of times the gateway should retry sending
    /// requests when errors such as timeouts or 502 errors occur.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub retries: Option<i64>,
    /// Scheme is the protocol used to communicate with the upstream.
    /// Default is `http`.
    /// Can be `http`, `https`, `grpc`, or `grpcs`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scheme: Option<BackendTrafficPolicyScheme>,
    /// TargetRef identifies an API object to apply policy to.
    /// Currently, Backends (i.e. Service, ServiceImport, or any
    /// implementation-specific backendRef) are the only valid API
    /// target references.
    #[serde(rename = "targetRefs")]
    pub target_refs: Vec<BackendTrafficPolicyTargetRefs>,
    /// Timeout sets the read, send, and connect timeouts to the upstream.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timeout: Option<BackendTrafficPolicyTimeout>,
    /// UpstreamHost specifies the host of the Upstream request. Used only if
    /// passHost is set to `rewrite`.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "upstreamHost")]
    pub upstream_host: Option<String>,
}

/// HealthCheck defines active and passive health check configuration for
/// the upstream backends. When configured, APISIX will probe backends
/// (active) or monitor live traffic (passive) to detect and bypass
/// unhealthy nodes.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BackendTrafficPolicyHealthCheck {
    /// Active health checks proactively send requests to upstream nodes to determine their availability.
    pub active: BackendTrafficPolicyHealthCheckActive,
    /// Passive health checks evaluate upstream health based on observed traffic (timeouts, errors).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub passive: Option<BackendTrafficPolicyHealthCheckPassive>,
}

/// Active health checks proactively send requests to upstream nodes to determine their availability.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BackendTrafficPolicyHealthCheckActive {
    /// Concurrency sets the number of targets to be checked at the same time.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub concurrency: Option<i64>,
    /// Healthy configures the thresholds for marking a node healthy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub healthy: Option<BackendTrafficPolicyHealthCheckActiveHealthy>,
    /// Host sets the upstream host used in the health check request.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub host: Option<String>,
    /// HTTPPath sets the HTTP path for the probe request.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "httpPath")]
    pub http_path: Option<String>,
    /// Port sets the port on the upstream node to probe.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub port: Option<i32>,
    /// RequestHeaders sets additional HTTP request headers for the probe.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "requestHeaders")]
    pub request_headers: Option<Vec<String>>,
    /// StrictTLS controls whether TLS certificate validation is enforced.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "strictTLS")]
    pub strict_tls: Option<bool>,
    /// Timeout sets health check timeout.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timeout: Option<String>,
    /// Type is the health check type. Can be `http`, `https`, or `tcp`.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "type")]
    pub r#type: Option<BackendTrafficPolicyHealthCheckActiveType>,
    /// Unhealthy configures the thresholds for marking a node unhealthy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub unhealthy: Option<BackendTrafficPolicyHealthCheckActiveUnhealthy>,
}

/// Healthy configures the thresholds for marking a node healthy.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BackendTrafficPolicyHealthCheckActiveHealthy {
    /// HTTPCodes is the list of HTTP status codes considered healthy.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "httpCodes")]
    pub http_codes: Option<Vec<i64>>,
    /// Interval defines the time between health check probes.
    /// Minimum is 1s.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub interval: Option<String>,
    /// Successes is the number of consecutive successful responses required to mark a node healthy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub successes: Option<i64>,
}

/// Active health checks proactively send requests to upstream nodes to determine their availability.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BackendTrafficPolicyHealthCheckActiveType {
    #[serde(rename = "http")]
    Http,
    #[serde(rename = "https")]
    Https,
    #[serde(rename = "tcp")]
    Tcp,
}

/// Unhealthy configures the thresholds for marking a node unhealthy.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BackendTrafficPolicyHealthCheckActiveUnhealthy {
    /// HTTPCodes is the list of HTTP status codes considered unhealthy.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "httpCodes")]
    pub http_codes: Option<Vec<i64>>,
    /// HTTPFailures is the number of HTTP failures to mark a node unhealthy.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "httpFailures")]
    pub http_failures: Option<i64>,
    /// Interval defines the time between health check probes.
    /// Minimum is 1s.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub interval: Option<String>,
    /// TCPFailures is the number of TCP failures to mark a node unhealthy.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "tcpFailures")]
    pub tcp_failures: Option<i64>,
    /// Timeouts is the number of timeouts to mark a node unhealthy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timeouts: Option<i64>,
}

/// Passive health checks evaluate upstream health based on observed traffic (timeouts, errors).
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BackendTrafficPolicyHealthCheckPassive {
    /// Healthy defines conditions under which a node is considered healthy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub healthy: Option<BackendTrafficPolicyHealthCheckPassiveHealthy>,
    /// Type is the passive health check type. Can be `http`, `https`, or `tcp`.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "type")]
    pub r#type: Option<BackendTrafficPolicyHealthCheckPassiveType>,
    /// Unhealthy defines conditions under which a node is considered unhealthy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub unhealthy: Option<BackendTrafficPolicyHealthCheckPassiveUnhealthy>,
}

/// Healthy defines conditions under which a node is considered healthy.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BackendTrafficPolicyHealthCheckPassiveHealthy {
    /// HTTPCodes is the list of HTTP status codes considered healthy.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "httpCodes")]
    pub http_codes: Option<Vec<i64>>,
    /// Successes is the number of consecutive successful responses required to mark a node healthy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub successes: Option<i64>,
}

/// Passive health checks evaluate upstream health based on observed traffic (timeouts, errors).
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BackendTrafficPolicyHealthCheckPassiveType {
    #[serde(rename = "http")]
    Http,
    #[serde(rename = "https")]
    Https,
    #[serde(rename = "tcp")]
    Tcp,
}

/// Unhealthy defines conditions under which a node is considered unhealthy.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BackendTrafficPolicyHealthCheckPassiveUnhealthy {
    /// HTTPCodes is the list of HTTP status codes considered unhealthy.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "httpCodes")]
    pub http_codes: Option<Vec<i64>>,
    /// HTTPFailures is the number of HTTP failures to mark a node unhealthy.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "httpFailures")]
    pub http_failures: Option<i64>,
    /// TCPFailures is the number of TCP failures to mark a node unhealthy.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "tcpFailures")]
    pub tcp_failures: Option<i64>,
    /// Timeouts is the number of timeouts to mark a node unhealthy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timeouts: Option<i64>,
}

/// LoadBalancer represents the load balancer configuration for Kubernetes Service.
/// The default strategy is round robin.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub struct BackendTrafficPolicyLoadbalancer {
    /// HashOn specified the type of field used for hashing, required when type is `chash`.
    /// Default is `vars`.
    /// Can be `vars`, `header`, `cookie`, `consumer`, or `vars_combinations`.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "hashOn")]
    pub hash_on: Option<BackendTrafficPolicyLoadbalancerHashOn>,
    /// Key is used with HashOn, generally required when type is `chash`.
    /// When HashOn is `header` or `cookie`, specifies the name of the header or cookie.
    /// When HashOn is `consumer`, key is not required, as the consumer name is used automatically.
    /// When HashOn is `vars` or `vars_combinations`, key refers to one or a combination of
    /// [APISIX variable](<https://apisix.apache.org/docs/apisix/apisix-variable/).>
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key: Option<String>,
    /// Type specifies the load balancing algorithms to route traffic to the backend.
    /// Default is `roundrobin`.
    /// Can be `roundrobin`, `chash`, `ewma`, or `least_conn`.
    #[serde(rename = "type")]
    pub r#type: BackendTrafficPolicyLoadbalancerType,
}

/// LoadBalancer represents the load balancer configuration for Kubernetes Service.
/// The default strategy is round robin.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BackendTrafficPolicyLoadbalancerHashOn {
    #[serde(rename = "vars")]
    Vars,
    #[serde(rename = "header")]
    Header,
    #[serde(rename = "cookie")]
    Cookie,
    #[serde(rename = "consumer")]
    Consumer,
    #[serde(rename = "vars_combinations")]
    VarsCombinations,
}

/// LoadBalancer represents the load balancer configuration for Kubernetes Service.
/// The default strategy is round robin.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BackendTrafficPolicyLoadbalancerType {
    #[serde(rename = "roundrobin")]
    Roundrobin,
    #[serde(rename = "chash")]
    Chash,
    #[serde(rename = "ewma")]
    Ewma,
    #[serde(rename = "least_conn")]
    LeastConn,
}

/// BackendTrafficPolicySpec defines traffic handling policies applied to backend services,
/// such as load balancing strategy, connection settings, and failover behavior.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BackendTrafficPolicyPassHost {
    #[serde(rename = "pass")]
    Pass,
    #[serde(rename = "node")]
    Node,
    #[serde(rename = "rewrite")]
    Rewrite,
}

/// BackendTrafficPolicySpec defines traffic handling policies applied to backend services,
/// such as load balancing strategy, connection settings, and failover behavior.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BackendTrafficPolicyScheme {
    #[serde(rename = "http")]
    Http,
    #[serde(rename = "https")]
    Https,
    #[serde(rename = "grpc")]
    Grpc,
    #[serde(rename = "grpcs")]
    Grpcs,
}

/// LocalPolicyTargetReferenceWithSectionName identifies an API object to apply a
/// direct policy to. This should be used as part of Policy resources that can
/// target single resources. For more information on how this policy attachment
/// mode works, and a sample Policy resource, refer to the policy attachment
/// documentation for Gateway API.
/// 
/// Note: This should only be used for direct policy attachment when references
/// to SectionName are actually needed. In all other cases,
/// LocalPolicyTargetReference should be used.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BackendTrafficPolicyTargetRefs {
    /// Group is the group of the target resource.
    pub group: String,
    /// Kind is kind of the target resource.
    pub kind: String,
    /// Name is the name of the target resource.
    pub name: String,
    /// SectionName is the name of a section within the target resource. When
    /// unspecified, this targetRef targets the entire resource. In the following
    /// resources, SectionName is interpreted as the following:
    /// 
    /// * Gateway: Listener name
    /// * HTTPRoute: HTTPRouteRule name
    /// * Service: Port name
    /// 
    /// If a SectionName is specified, but does not exist on the targeted object,
    /// the Policy must fail to attach, and the policy implementation should record
    /// a `ResolvedRefs` or similar Condition in the Policy's status.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "sectionName")]
    pub section_name: Option<String>,
}

/// Timeout sets the read, send, and connect timeouts to the upstream.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BackendTrafficPolicyTimeout {
    /// Connection timeout. Default is `60s`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connect: Option<String>,
    /// Read timeout. Default is `60s`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub read: Option<String>,
    /// Send timeout. Default is `60s`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub send: Option<String>,
}

/// PolicyStatus defines the common attributes that all Policies should include within
/// their status.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BackendTrafficPolicyStatus {
    /// Ancestors is a list of ancestor resources (usually Gateways) that are
    /// associated with the policy, and the status of the policy with respect to
    /// each ancestor. When this policy attaches to a parent, the controller that
    /// manages the parent and the ancestors MUST add an entry to this list when
    /// the controller first sees the policy and SHOULD update the entry as
    /// appropriate when the relevant ancestor is modified.
    /// 
    /// Note that choosing the relevant ancestor is left to the Policy designers;
    /// an important part of Policy design is designing the right object level at
    /// which to namespace this status.
    /// 
    /// Note also that implementations MUST ONLY populate ancestor status for
    /// the Ancestor resources they are responsible for. Implementations MUST
    /// use the ControllerName field to uniquely identify the entries in this list
    /// that they are responsible for.
    /// 
    /// Note that to achieve this, the list of PolicyAncestorStatus structs
    /// MUST be treated as a map with a composite key, made up of the AncestorRef
    /// and ControllerName fields combined.
    /// 
    /// A maximum of 16 ancestors will be represented in this list. An empty list
    /// means the Policy is not relevant for any ancestors.
    /// 
    /// If this slice is full, implementations MUST NOT add further entries.
    /// Instead they MUST consider the policy unimplementable and signal that
    /// on any related resources such as the ancestor that would be referenced
    /// here. For example, if this list was full on BackendTLSPolicy, no
    /// additional Gateways would be able to reference the Service targeted by
    /// the BackendTLSPolicy.
    pub ancestors: Vec<BackendTrafficPolicyStatusAncestors>,
}

/// PolicyAncestorStatus describes the status of a route with respect to an
/// associated Ancestor.
/// 
/// Ancestors refer to objects that are either the Target of a policy or above it
/// in terms of object hierarchy. For example, if a policy targets a Service, the
/// Policy's Ancestors are, in order, the Service, the HTTPRoute, the Gateway, and
/// the GatewayClass. Almost always, in this hierarchy, the Gateway will be the most
/// useful object to place Policy status on, so we recommend that implementations
/// SHOULD use Gateway as the PolicyAncestorStatus object unless the designers
/// have a _very_ good reason otherwise.
/// 
/// In the context of policy attachment, the Ancestor is used to distinguish which
/// resource results in a distinct application of this policy. For example, if a policy
/// targets a Service, it may have a distinct result per attached Gateway.
/// 
/// Policies targeting the same resource may have different effects depending on the
/// ancestors of those resources. For example, different Gateways targeting the same
/// Service may have different capabilities, especially if they have different underlying
/// implementations.
/// 
/// For example, in BackendTLSPolicy, the Policy attaches to a Service that is
/// used as a backend in a HTTPRoute that is itself attached to a Gateway.
/// In this case, the relevant object for status is the Gateway, and that is the
/// ancestor object referred to in this status.
/// 
/// Note that a parent is also an ancestor, so for objects where the parent is the
/// relevant object for status, this struct SHOULD still be used.
/// 
/// This struct is intended to be used in a slice that's effectively a map,
/// with a composite key made up of the AncestorRef and the ControllerName.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BackendTrafficPolicyStatusAncestors {
    /// AncestorRef corresponds with a ParentRef in the spec that this
    /// PolicyAncestorStatus struct describes the status of.
    #[serde(rename = "ancestorRef")]
    pub ancestor_ref: BackendTrafficPolicyStatusAncestorsAncestorRef,
    /// Conditions describes the status of the Policy with respect to the given Ancestor.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub conditions: Option<Vec<Condition>>,
    /// ControllerName is a domain/path string that indicates the name of the
    /// controller that wrote this status. This corresponds with the
    /// controllerName field on GatewayClass.
    /// 
    /// Example: "example.net/gateway-controller".
    /// 
    /// The format of this field is DOMAIN "/" PATH, where DOMAIN and PATH are
    /// valid Kubernetes names
    /// (<https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names).>
    /// 
    /// Controllers MUST populate this field when writing status. Controllers should ensure that
    /// entries to status populated with their ControllerName are cleaned up when they are no
    /// longer necessary.
    #[serde(rename = "controllerName")]
    pub controller_name: String,
}

/// AncestorRef corresponds with a ParentRef in the spec that this
/// PolicyAncestorStatus struct describes the status of.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BackendTrafficPolicyStatusAncestorsAncestorRef {
    /// Group is the group of the referent.
    /// When unspecified, "gateway.networking.k8s.io" is inferred.
    /// To set the core API group (such as for a "Service" kind referent),
    /// Group must be explicitly set to "" (empty string).
    /// 
    /// Support: Core
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group: Option<String>,
    /// Kind is kind of the referent.
    /// 
    /// There are two kinds of parent resources with "Core" support:
    /// 
    /// * Gateway (Gateway conformance profile)
    /// * Service (Mesh conformance profile, ClusterIP Services only)
    /// 
    /// Support for other resources is Implementation-Specific.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind: Option<String>,
    /// Name is the name of the referent.
    /// 
    /// Support: Core
    pub name: String,
    /// Namespace is the namespace of the referent. When unspecified, this refers
    /// to the local namespace of the Route.
    /// 
    /// Note that there are specific rules for ParentRefs which cross namespace
    /// boundaries. Cross-namespace references are only valid if they are explicitly
    /// allowed by something in the namespace they are referring to. For example:
    /// Gateway has the AllowedRoutes field, and ReferenceGrant provides a
    /// generic way to enable any other kind of cross-namespace reference.
    /// 
    /// <gateway:experimental:description>
    /// ParentRefs from a Route to a Service in the same namespace are "producer"
    /// routes, which apply default routing rules to inbound connections from
    /// any namespace to the Service.
    /// 
    /// ParentRefs from a Route to a Service in a different namespace are
    /// "consumer" routes, and these routing rules are only applied to outbound
    /// connections originating from the same namespace as the Route, for which
    /// the intended destination of the connections are a Service targeted as a
    /// ParentRef of the Route.
    /// </gateway:experimental:description>
    /// 
    /// Support: Core
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub namespace: Option<String>,
    /// Port is the network port this Route targets. It can be interpreted
    /// differently based on the type of parent resource.
    /// 
    /// When the parent resource is a Gateway, this targets all listeners
    /// listening on the specified port that also support this kind of Route(and
    /// select this Route). It's not recommended to set `Port` unless the
    /// networking behaviors specified in a Route must apply to a specific port
    /// as opposed to a listener(s) whose port(s) may be changed. When both Port
    /// and SectionName are specified, the name and port of the selected listener
    /// must match both specified values.
    /// 
    /// <gateway:experimental:description>
    /// When the parent resource is a Service, this targets a specific port in the
    /// Service spec. When both Port (experimental) and SectionName are specified,
    /// the name and port of the selected port must match both specified values.
    /// </gateway:experimental:description>
    /// 
    /// Implementations MAY choose to support other parent resources.
    /// Implementations supporting other types of parent resources MUST clearly
    /// document how/if Port is interpreted.
    /// 
    /// For the purpose of status, an attachment is considered successful as
    /// long as the parent resource accepts it partially. For example, Gateway
    /// listeners can restrict which Routes can attach to them by Route kind,
    /// namespace, or hostname. If 1 of 2 Gateway listeners accept attachment
    /// from the referencing Route, the Route MUST be considered successfully
    /// attached. If no Gateway listeners accept attachment from this Route,
    /// the Route MUST be considered detached from the Gateway.
    /// 
    /// Support: Extended
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub port: Option<i32>,
    /// SectionName is the name of a section within the target resource. In the
    /// following resources, SectionName is interpreted as the following:
    /// 
    /// * Gateway: Listener name. When both Port (experimental) and SectionName
    /// are specified, the name and port of the selected listener must match
    /// both specified values.
    /// * Service: Port name. When both Port (experimental) and SectionName
    /// are specified, the name and port of the selected listener must match
    /// both specified values.
    /// 
    /// Implementations MAY choose to support attaching Routes to other resources.
    /// If that is the case, they MUST clearly document how SectionName is
    /// interpreted.
    /// 
    /// When unspecified (empty string), this will reference the entire resource.
    /// For the purpose of status, an attachment is considered successful if at
    /// least one section in the parent resource accepts it. For example, Gateway
    /// listeners can restrict which Routes can attach to them by Route kind,
    /// namespace, or hostname. If 1 of 2 Gateway listeners accept attachment from
    /// the referencing Route, the Route MUST be considered successfully
    /// attached. If no Gateway listeners accept attachment from this Route, the
    /// Route MUST be considered detached from the Gateway.
    /// 
    /// Support: Core
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "sectionName")]
    pub section_name: Option<String>,
}

