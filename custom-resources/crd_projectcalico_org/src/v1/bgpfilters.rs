// WARNING: generated by kopium - manual changes will be overwritten
// kopium command: kopium --docs --derive=Default --derive=PartialEq --smart-derive-elision --filename crd-catalog/projectcalico/calico/crd.projectcalico.org/v1/bgpfilters.yaml
// kopium version: 0.23.0

#[allow(unused_imports)]
mod prelude {
    pub use kube::CustomResource;
    pub use serde::{Serialize, Deserialize};
}

use self::prelude::*;

/// BGPFilterSpec contains the IPv4 and IPv6 filter rules of the BGP Filter.
#[derive(CustomResource, Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
#[kube(group = "crd.projectcalico.org", version = "v1", kind = "BGPFilter", root = "BgpFilter", plural = "bgpfilters")]
#[kube(schema = "disabled")]
#[kube(derive="Default")]
#[kube(derive="PartialEq")]
pub struct BgpFilterSpec {
    /// The ordered set of IPv4 BGPFilter rules acting on exporting routes to a peer.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "exportV4")]
    pub export_v4: Option<Vec<BgpFilterExportV4>>,
    /// The ordered set of IPv6 BGPFilter rules acting on exporting routes to a peer.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "exportV6")]
    pub export_v6: Option<Vec<BgpFilterExportV6>>,
    /// The ordered set of IPv4 BGPFilter rules acting on importing routes from a peer.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "importV4")]
    pub import_v4: Option<Vec<BgpFilterImportV4>>,
    /// The ordered set of IPv6 BGPFilter rules acting on importing routes from a peer.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "importV6")]
    pub import_v6: Option<Vec<BgpFilterImportV6>>,
}

/// BGPFilterRuleV4 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub struct BgpFilterExportV4 {
    pub action: BgpFilterExportV4Action,
    /// If non-empty, this filter rule will only apply to routes whose AS path begins with the
    /// specified sequence of AS numbers.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "asPathPrefix")]
    pub as_path_prefix: Option<Vec<i64>>,
    /// If non-empty, this filter rule will only apply when the route being exported or imported
    /// "matches" the given CIDR - where the definition of "matches" is according to
    /// MatchOperator and PrefixLength.  CIDR should be in conventional CIDR notation,
    /// <prefix>/<length>.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cidr: Option<String>,
    /// If set, this filter rule will only apply to routes that carry the specified BGP
    /// community.  On import, this matches communities set by the remote peer.  On export,
    /// this matches communities already present on the route, whether received from a BGP
    /// peer (e.g. on a route reflector re-advertising to an eBGP peer) or added locally
    /// by an import filter or an earlier export rule's AddCommunity operation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub communities: Option<BgpFilterExportV4Communities>,
    /// If non-empty, this filter rule will only apply to routes with an outgoing interface that
    /// matches Interface.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub interface: Option<String>,
    /// MatchOperator defines how the route's prefix is compared against CIDR.  "Equal" requires
    /// an exact prefix match, "In" requires the route to be contained within the CIDR (or equal),
    /// "NotEqual" and "NotIn" are their negations.  Only meaningful when CIDR is also specified.
    /// Required when CIDR is set.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchOperator")]
    pub match_operator: Option<BgpFilterExportV4MatchOperator>,
    /// Operations is an ordered list of route modifications to apply to matching routes before
    /// accepting them.  Only valid when Action is "Accept"; specifying operations with "Reject"
    /// is rejected by validation.  Each entry must set exactly one operation field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub operations: Option<Vec<BgpFilterExportV4Operations>>,
    /// If non-empty, this filter rule will only apply to routes being imported from or exported
    /// to a BGP peer of the specified type.  If empty, the rule applies to all peers.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "peerType")]
    pub peer_type: Option<BgpFilterExportV4PeerType>,
    /// PrefixLength further constrains the CIDR match by restricting the range of allowed
    /// prefix lengths.  For example, CIDR "10.0.0.0/8" with MatchOperator "In" and
    /// PrefixLength {min: 16, max: 24} matches any route within 10.0.0.0/8 whose prefix
    /// length is between /16 and /24.  Requires CIDR to be set; if CIDR is omitted,
    /// PrefixLength is ignored.  If PrefixLength is nil and CIDR is set, the CIDR's own
    /// prefix length is used as the minimum and /32 (for V4) as the maximum.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "prefixLength")]
    pub prefix_length: Option<BgpFilterExportV4PrefixLength>,
    /// If set, this filter rule will only apply to routes with the given priority, in the
    /// same units as the ...RoutePriority fields in FelixConfiguration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub priority: Option<i64>,
    /// If set to "RemotePeers": for export rules, this filter rule will only apply to routes
    /// learned from BGP peers (i.e. re-advertised routes), not locally originated routes.
    /// For import rules, this field is redundant because imported routes are by definition
    /// from BGP peers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source: Option<BgpFilterExportV4Source>,
}

/// BGPFilterRuleV4 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BgpFilterExportV4Action {
    Accept,
    Reject,
}

/// If set, this filter rule will only apply to routes that carry the specified BGP
/// community.  On import, this matches communities set by the remote peer.  On export,
/// this matches communities already present on the route, whether received from a BGP
/// peer (e.g. on a route reflector re-advertising to an eBGP peer) or added locally
/// by an import filter or an earlier export rule's AddCommunity operation.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterExportV4Communities {
    /// Values is a list of BGP community values to match against. Exactly one value must be specified.
    pub values: Vec<String>,
}

/// BGPFilterRuleV4 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BgpFilterExportV4MatchOperator {
    Equal,
    NotEqual,
    In,
    NotIn,
}

/// BGPFilterOperation is a discriminated union representing a single route modification.
/// Exactly one field must be set.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterExportV4Operations {
    /// AddCommunity adds the specified BGP community to the route.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "addCommunity")]
    pub add_community: Option<BgpFilterExportV4OperationsAddCommunity>,
    /// PrependASPath prepends the specified AS numbers to the route's AS path.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "prependASPath")]
    pub prepend_as_path: Option<BgpFilterExportV4OperationsPrependAsPath>,
    /// SetPriority sets the route's priority (metric), in the same units as the
    /// ...RoutePriority fields in FelixConfiguration.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "setPriority")]
    pub set_priority: Option<BgpFilterExportV4OperationsSetPriority>,
}

/// AddCommunity adds the specified BGP community to the route.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterExportV4OperationsAddCommunity {
    /// Value is the BGP community to add.
    pub value: String,
}

/// PrependASPath prepends the specified AS numbers to the route's AS path.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterExportV4OperationsPrependAsPath {
    /// Prefix is the sequence of AS numbers to prepend to the route's AS path.
    /// The resulting path starts with these AS numbers in the order listed;
    /// e.g. [65000, 65001] produces the path "65000 65001 <original>".
    pub prefix: Vec<i64>,
}

/// SetPriority sets the route's priority (metric), in the same units as the
/// ...RoutePriority fields in FelixConfiguration.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterExportV4OperationsSetPriority {
    /// Value is the priority to set, in the same units as FelixConfiguration's
    /// ...RoutePriority fields.
    pub value: i64,
}

/// BGPFilterRuleV4 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BgpFilterExportV4PeerType {
    #[serde(rename = "eBGP")]
    EBgp,
    #[serde(rename = "iBGP")]
    IBgp,
}

/// PrefixLength further constrains the CIDR match by restricting the range of allowed
/// prefix lengths.  For example, CIDR "10.0.0.0/8" with MatchOperator "In" and
/// PrefixLength {min: 16, max: 24} matches any route within 10.0.0.0/8 whose prefix
/// length is between /16 and /24.  Requires CIDR to be set; if CIDR is omitted,
/// PrefixLength is ignored.  If PrefixLength is nil and CIDR is set, the CIDR's own
/// prefix length is used as the minimum and /32 (for V4) as the maximum.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterExportV4PrefixLength {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub max: Option<i32>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min: Option<i32>,
}

/// BGPFilterRuleV4 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BgpFilterExportV4Source {
    RemotePeers,
}

/// BGPFilterRuleV6 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub struct BgpFilterExportV6 {
    pub action: BgpFilterExportV6Action,
    /// If non-empty, this filter rule will only apply to routes whose AS path begins with the
    /// specified sequence of AS numbers.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "asPathPrefix")]
    pub as_path_prefix: Option<Vec<i64>>,
    /// If non-empty, this filter rule will only apply when the route being exported or imported
    /// "matches" the given CIDR - where the definition of "matches" is according to
    /// MatchOperator and PrefixLength.  CIDR should be in conventional CIDR notation,
    /// <prefix>/<length>.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cidr: Option<String>,
    /// If set, this filter rule will only apply to routes that carry the specified BGP
    /// community.  On import, this matches communities set by the remote peer.  On export,
    /// this matches communities already present on the route, whether received from a BGP
    /// peer (e.g. on a route reflector re-advertising to an eBGP peer) or added locally
    /// by an import filter or an earlier export rule's AddCommunity operation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub communities: Option<BgpFilterExportV6Communities>,
    /// If non-empty, this filter rule will only apply to routes with an outgoing interface that
    /// matches Interface.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub interface: Option<String>,
    /// MatchOperator defines how the route's prefix is compared against CIDR.  "Equal" requires
    /// an exact prefix match, "In" requires the route to be contained within the CIDR (or equal),
    /// "NotEqual" and "NotIn" are their negations.  Only meaningful when CIDR is also specified.
    /// Required when CIDR is set.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchOperator")]
    pub match_operator: Option<BgpFilterExportV6MatchOperator>,
    /// Operations is an ordered list of route modifications to apply to matching routes before
    /// accepting them.  Only valid when Action is "Accept"; specifying operations with "Reject"
    /// is rejected by validation.  Each entry must set exactly one operation field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub operations: Option<Vec<BgpFilterExportV6Operations>>,
    /// If non-empty, this filter rule will only apply to routes being imported from or exported
    /// to a BGP peer of the specified type.  If empty, the rule applies to all peers.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "peerType")]
    pub peer_type: Option<BgpFilterExportV6PeerType>,
    /// PrefixLength further constrains the CIDR match by restricting the range of allowed
    /// prefix lengths.  For example, CIDR "fd00::/8" with MatchOperator "In" and
    /// PrefixLength {min: 48, max: 64} matches any route within fd00::/8 whose prefix
    /// length is between /48 and /64.  Requires CIDR to be set; if CIDR is omitted,
    /// PrefixLength is ignored.  If PrefixLength is nil and CIDR is set, the CIDR's own
    /// prefix length is used as the minimum and /128 (for V6) as the maximum.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "prefixLength")]
    pub prefix_length: Option<BgpFilterExportV6PrefixLength>,
    /// If set, this filter rule will only apply to routes with the given priority, in the
    /// same units as the ...RoutePriority fields in FelixConfiguration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub priority: Option<i64>,
    /// If set to "RemotePeers": for export rules, this filter rule will only apply to routes
    /// learned from BGP peers (i.e. re-advertised routes), not locally originated routes.
    /// For import rules, this field is redundant because imported routes are by definition
    /// from BGP peers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source: Option<BgpFilterExportV6Source>,
}

/// BGPFilterRuleV6 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BgpFilterExportV6Action {
    Accept,
    Reject,
}

/// If set, this filter rule will only apply to routes that carry the specified BGP
/// community.  On import, this matches communities set by the remote peer.  On export,
/// this matches communities already present on the route, whether received from a BGP
/// peer (e.g. on a route reflector re-advertising to an eBGP peer) or added locally
/// by an import filter or an earlier export rule's AddCommunity operation.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterExportV6Communities {
    /// Values is a list of BGP community values to match against. Exactly one value must be specified.
    pub values: Vec<String>,
}

/// BGPFilterRuleV6 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BgpFilterExportV6MatchOperator {
    Equal,
    NotEqual,
    In,
    NotIn,
}

/// BGPFilterOperation is a discriminated union representing a single route modification.
/// Exactly one field must be set.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterExportV6Operations {
    /// AddCommunity adds the specified BGP community to the route.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "addCommunity")]
    pub add_community: Option<BgpFilterExportV6OperationsAddCommunity>,
    /// PrependASPath prepends the specified AS numbers to the route's AS path.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "prependASPath")]
    pub prepend_as_path: Option<BgpFilterExportV6OperationsPrependAsPath>,
    /// SetPriority sets the route's priority (metric), in the same units as the
    /// ...RoutePriority fields in FelixConfiguration.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "setPriority")]
    pub set_priority: Option<BgpFilterExportV6OperationsSetPriority>,
}

/// AddCommunity adds the specified BGP community to the route.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterExportV6OperationsAddCommunity {
    /// Value is the BGP community to add.
    pub value: String,
}

/// PrependASPath prepends the specified AS numbers to the route's AS path.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterExportV6OperationsPrependAsPath {
    /// Prefix is the sequence of AS numbers to prepend to the route's AS path.
    /// The resulting path starts with these AS numbers in the order listed;
    /// e.g. [65000, 65001] produces the path "65000 65001 <original>".
    pub prefix: Vec<i64>,
}

/// SetPriority sets the route's priority (metric), in the same units as the
/// ...RoutePriority fields in FelixConfiguration.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterExportV6OperationsSetPriority {
    /// Value is the priority to set, in the same units as FelixConfiguration's
    /// ...RoutePriority fields.
    pub value: i64,
}

/// BGPFilterRuleV6 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BgpFilterExportV6PeerType {
    #[serde(rename = "eBGP")]
    EBgp,
    #[serde(rename = "iBGP")]
    IBgp,
}

/// PrefixLength further constrains the CIDR match by restricting the range of allowed
/// prefix lengths.  For example, CIDR "fd00::/8" with MatchOperator "In" and
/// PrefixLength {min: 48, max: 64} matches any route within fd00::/8 whose prefix
/// length is between /48 and /64.  Requires CIDR to be set; if CIDR is omitted,
/// PrefixLength is ignored.  If PrefixLength is nil and CIDR is set, the CIDR's own
/// prefix length is used as the minimum and /128 (for V6) as the maximum.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterExportV6PrefixLength {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub max: Option<i32>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min: Option<i32>,
}

/// BGPFilterRuleV6 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BgpFilterExportV6Source {
    RemotePeers,
}

/// BGPFilterRuleV4 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub struct BgpFilterImportV4 {
    pub action: BgpFilterImportV4Action,
    /// If non-empty, this filter rule will only apply to routes whose AS path begins with the
    /// specified sequence of AS numbers.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "asPathPrefix")]
    pub as_path_prefix: Option<Vec<i64>>,
    /// If non-empty, this filter rule will only apply when the route being exported or imported
    /// "matches" the given CIDR - where the definition of "matches" is according to
    /// MatchOperator and PrefixLength.  CIDR should be in conventional CIDR notation,
    /// <prefix>/<length>.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cidr: Option<String>,
    /// If set, this filter rule will only apply to routes that carry the specified BGP
    /// community.  On import, this matches communities set by the remote peer.  On export,
    /// this matches communities already present on the route, whether received from a BGP
    /// peer (e.g. on a route reflector re-advertising to an eBGP peer) or added locally
    /// by an import filter or an earlier export rule's AddCommunity operation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub communities: Option<BgpFilterImportV4Communities>,
    /// If non-empty, this filter rule will only apply to routes with an outgoing interface that
    /// matches Interface.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub interface: Option<String>,
    /// MatchOperator defines how the route's prefix is compared against CIDR.  "Equal" requires
    /// an exact prefix match, "In" requires the route to be contained within the CIDR (or equal),
    /// "NotEqual" and "NotIn" are their negations.  Only meaningful when CIDR is also specified.
    /// Required when CIDR is set.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchOperator")]
    pub match_operator: Option<BgpFilterImportV4MatchOperator>,
    /// Operations is an ordered list of route modifications to apply to matching routes before
    /// accepting them.  Only valid when Action is "Accept"; specifying operations with "Reject"
    /// is rejected by validation.  Each entry must set exactly one operation field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub operations: Option<Vec<BgpFilterImportV4Operations>>,
    /// If non-empty, this filter rule will only apply to routes being imported from or exported
    /// to a BGP peer of the specified type.  If empty, the rule applies to all peers.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "peerType")]
    pub peer_type: Option<BgpFilterImportV4PeerType>,
    /// PrefixLength further constrains the CIDR match by restricting the range of allowed
    /// prefix lengths.  For example, CIDR "10.0.0.0/8" with MatchOperator "In" and
    /// PrefixLength {min: 16, max: 24} matches any route within 10.0.0.0/8 whose prefix
    /// length is between /16 and /24.  Requires CIDR to be set; if CIDR is omitted,
    /// PrefixLength is ignored.  If PrefixLength is nil and CIDR is set, the CIDR's own
    /// prefix length is used as the minimum and /32 (for V4) as the maximum.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "prefixLength")]
    pub prefix_length: Option<BgpFilterImportV4PrefixLength>,
    /// If set, this filter rule will only apply to routes with the given priority, in the
    /// same units as the ...RoutePriority fields in FelixConfiguration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub priority: Option<i64>,
    /// If set to "RemotePeers": for export rules, this filter rule will only apply to routes
    /// learned from BGP peers (i.e. re-advertised routes), not locally originated routes.
    /// For import rules, this field is redundant because imported routes are by definition
    /// from BGP peers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source: Option<BgpFilterImportV4Source>,
}

/// BGPFilterRuleV4 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BgpFilterImportV4Action {
    Accept,
    Reject,
}

/// If set, this filter rule will only apply to routes that carry the specified BGP
/// community.  On import, this matches communities set by the remote peer.  On export,
/// this matches communities already present on the route, whether received from a BGP
/// peer (e.g. on a route reflector re-advertising to an eBGP peer) or added locally
/// by an import filter or an earlier export rule's AddCommunity operation.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterImportV4Communities {
    /// Values is a list of BGP community values to match against. Exactly one value must be specified.
    pub values: Vec<String>,
}

/// BGPFilterRuleV4 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BgpFilterImportV4MatchOperator {
    Equal,
    NotEqual,
    In,
    NotIn,
}

/// BGPFilterOperation is a discriminated union representing a single route modification.
/// Exactly one field must be set.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterImportV4Operations {
    /// AddCommunity adds the specified BGP community to the route.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "addCommunity")]
    pub add_community: Option<BgpFilterImportV4OperationsAddCommunity>,
    /// PrependASPath prepends the specified AS numbers to the route's AS path.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "prependASPath")]
    pub prepend_as_path: Option<BgpFilterImportV4OperationsPrependAsPath>,
    /// SetPriority sets the route's priority (metric), in the same units as the
    /// ...RoutePriority fields in FelixConfiguration.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "setPriority")]
    pub set_priority: Option<BgpFilterImportV4OperationsSetPriority>,
}

/// AddCommunity adds the specified BGP community to the route.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterImportV4OperationsAddCommunity {
    /// Value is the BGP community to add.
    pub value: String,
}

/// PrependASPath prepends the specified AS numbers to the route's AS path.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterImportV4OperationsPrependAsPath {
    /// Prefix is the sequence of AS numbers to prepend to the route's AS path.
    /// The resulting path starts with these AS numbers in the order listed;
    /// e.g. [65000, 65001] produces the path "65000 65001 <original>".
    pub prefix: Vec<i64>,
}

/// SetPriority sets the route's priority (metric), in the same units as the
/// ...RoutePriority fields in FelixConfiguration.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterImportV4OperationsSetPriority {
    /// Value is the priority to set, in the same units as FelixConfiguration's
    /// ...RoutePriority fields.
    pub value: i64,
}

/// BGPFilterRuleV4 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BgpFilterImportV4PeerType {
    #[serde(rename = "eBGP")]
    EBgp,
    #[serde(rename = "iBGP")]
    IBgp,
}

/// PrefixLength further constrains the CIDR match by restricting the range of allowed
/// prefix lengths.  For example, CIDR "10.0.0.0/8" with MatchOperator "In" and
/// PrefixLength {min: 16, max: 24} matches any route within 10.0.0.0/8 whose prefix
/// length is between /16 and /24.  Requires CIDR to be set; if CIDR is omitted,
/// PrefixLength is ignored.  If PrefixLength is nil and CIDR is set, the CIDR's own
/// prefix length is used as the minimum and /32 (for V4) as the maximum.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterImportV4PrefixLength {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub max: Option<i32>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min: Option<i32>,
}

/// BGPFilterRuleV4 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BgpFilterImportV4Source {
    RemotePeers,
}

/// BGPFilterRuleV6 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub struct BgpFilterImportV6 {
    pub action: BgpFilterImportV6Action,
    /// If non-empty, this filter rule will only apply to routes whose AS path begins with the
    /// specified sequence of AS numbers.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "asPathPrefix")]
    pub as_path_prefix: Option<Vec<i64>>,
    /// If non-empty, this filter rule will only apply when the route being exported or imported
    /// "matches" the given CIDR - where the definition of "matches" is according to
    /// MatchOperator and PrefixLength.  CIDR should be in conventional CIDR notation,
    /// <prefix>/<length>.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cidr: Option<String>,
    /// If set, this filter rule will only apply to routes that carry the specified BGP
    /// community.  On import, this matches communities set by the remote peer.  On export,
    /// this matches communities already present on the route, whether received from a BGP
    /// peer (e.g. on a route reflector re-advertising to an eBGP peer) or added locally
    /// by an import filter or an earlier export rule's AddCommunity operation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub communities: Option<BgpFilterImportV6Communities>,
    /// If non-empty, this filter rule will only apply to routes with an outgoing interface that
    /// matches Interface.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub interface: Option<String>,
    /// MatchOperator defines how the route's prefix is compared against CIDR.  "Equal" requires
    /// an exact prefix match, "In" requires the route to be contained within the CIDR (or equal),
    /// "NotEqual" and "NotIn" are their negations.  Only meaningful when CIDR is also specified.
    /// Required when CIDR is set.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchOperator")]
    pub match_operator: Option<BgpFilterImportV6MatchOperator>,
    /// Operations is an ordered list of route modifications to apply to matching routes before
    /// accepting them.  Only valid when Action is "Accept"; specifying operations with "Reject"
    /// is rejected by validation.  Each entry must set exactly one operation field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub operations: Option<Vec<BgpFilterImportV6Operations>>,
    /// If non-empty, this filter rule will only apply to routes being imported from or exported
    /// to a BGP peer of the specified type.  If empty, the rule applies to all peers.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "peerType")]
    pub peer_type: Option<BgpFilterImportV6PeerType>,
    /// PrefixLength further constrains the CIDR match by restricting the range of allowed
    /// prefix lengths.  For example, CIDR "fd00::/8" with MatchOperator "In" and
    /// PrefixLength {min: 48, max: 64} matches any route within fd00::/8 whose prefix
    /// length is between /48 and /64.  Requires CIDR to be set; if CIDR is omitted,
    /// PrefixLength is ignored.  If PrefixLength is nil and CIDR is set, the CIDR's own
    /// prefix length is used as the minimum and /128 (for V6) as the maximum.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "prefixLength")]
    pub prefix_length: Option<BgpFilterImportV6PrefixLength>,
    /// If set, this filter rule will only apply to routes with the given priority, in the
    /// same units as the ...RoutePriority fields in FelixConfiguration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub priority: Option<i64>,
    /// If set to "RemotePeers": for export rules, this filter rule will only apply to routes
    /// learned from BGP peers (i.e. re-advertised routes), not locally originated routes.
    /// For import rules, this field is redundant because imported routes are by definition
    /// from BGP peers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source: Option<BgpFilterImportV6Source>,
}

/// BGPFilterRuleV6 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BgpFilterImportV6Action {
    Accept,
    Reject,
}

/// If set, this filter rule will only apply to routes that carry the specified BGP
/// community.  On import, this matches communities set by the remote peer.  On export,
/// this matches communities already present on the route, whether received from a BGP
/// peer (e.g. on a route reflector re-advertising to an eBGP peer) or added locally
/// by an import filter or an earlier export rule's AddCommunity operation.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterImportV6Communities {
    /// Values is a list of BGP community values to match against. Exactly one value must be specified.
    pub values: Vec<String>,
}

/// BGPFilterRuleV6 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BgpFilterImportV6MatchOperator {
    Equal,
    NotEqual,
    In,
    NotIn,
}

/// BGPFilterOperation is a discriminated union representing a single route modification.
/// Exactly one field must be set.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterImportV6Operations {
    /// AddCommunity adds the specified BGP community to the route.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "addCommunity")]
    pub add_community: Option<BgpFilterImportV6OperationsAddCommunity>,
    /// PrependASPath prepends the specified AS numbers to the route's AS path.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "prependASPath")]
    pub prepend_as_path: Option<BgpFilterImportV6OperationsPrependAsPath>,
    /// SetPriority sets the route's priority (metric), in the same units as the
    /// ...RoutePriority fields in FelixConfiguration.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "setPriority")]
    pub set_priority: Option<BgpFilterImportV6OperationsSetPriority>,
}

/// AddCommunity adds the specified BGP community to the route.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterImportV6OperationsAddCommunity {
    /// Value is the BGP community to add.
    pub value: String,
}

/// PrependASPath prepends the specified AS numbers to the route's AS path.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterImportV6OperationsPrependAsPath {
    /// Prefix is the sequence of AS numbers to prepend to the route's AS path.
    /// The resulting path starts with these AS numbers in the order listed;
    /// e.g. [65000, 65001] produces the path "65000 65001 <original>".
    pub prefix: Vec<i64>,
}

/// SetPriority sets the route's priority (metric), in the same units as the
/// ...RoutePriority fields in FelixConfiguration.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterImportV6OperationsSetPriority {
    /// Value is the priority to set, in the same units as FelixConfiguration's
    /// ...RoutePriority fields.
    pub value: i64,
}

/// BGPFilterRuleV6 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BgpFilterImportV6PeerType {
    #[serde(rename = "eBGP")]
    EBgp,
    #[serde(rename = "iBGP")]
    IBgp,
}

/// PrefixLength further constrains the CIDR match by restricting the range of allowed
/// prefix lengths.  For example, CIDR "fd00::/8" with MatchOperator "In" and
/// PrefixLength {min: 48, max: 64} matches any route within fd00::/8 whose prefix
/// length is between /48 and /64.  Requires CIDR to be set; if CIDR is omitted,
/// PrefixLength is ignored.  If PrefixLength is nil and CIDR is set, the CIDR's own
/// prefix length is used as the minimum and /128 (for V6) as the maximum.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct BgpFilterImportV6PrefixLength {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub max: Option<i32>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min: Option<i32>,
}

/// BGPFilterRuleV6 defines a BGP filter rule consisting of match criteria, a terminal action,
/// and optional operations to apply to matching routes.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum BgpFilterImportV6Source {
    RemotePeers,
}

