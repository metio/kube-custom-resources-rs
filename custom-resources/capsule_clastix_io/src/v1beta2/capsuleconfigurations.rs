// WARNING: generated by kopium - manual changes will be overwritten
// kopium command: kopium --docs --derive=Default --derive=PartialEq --smart-derive-elision --filename crd-catalog/projectcapsule/capsule/capsule.clastix.io/v1beta2/capsuleconfigurations.yaml
// kopium version: 0.23.0

#[allow(unused_imports)]
mod prelude {
    pub use kube::CustomResource;
    pub use serde::{Serialize, Deserialize};
    pub use std::collections::BTreeMap;
}

use self::prelude::*;

/// CapsuleConfigurationSpec defines the Capsule configuration.
#[derive(CustomResource, Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
#[kube(group = "capsule.clastix.io", version = "v1beta2", kind = "CapsuleConfiguration", plural = "capsuleconfigurations")]
#[kube(status = "CapsuleConfigurationStatus")]
#[kube(schema = "disabled")]
#[kube(derive="Default")]
#[kube(derive="PartialEq")]
pub struct CapsuleConfigurationSpec {
    /// Define entities which can act as Administrators in the capsule construct
    /// These entities are automatically owners for all existing tenants. Meaning they can add namespaces to any tenant. However they must be specific by using the capsule label
    /// for interacting with namespaces. Because if that label is not defined, it's assumed that namespace interaction was not targeted towards a tenant and will therefor
    /// be ignored by capsule.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub administrators: Option<Vec<CapsuleConfigurationAdministrators>>,
    /// Configuration for dynamic Validating and Mutating Admission webhooks managed by Capsule.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub admission: Option<CapsuleConfigurationAdmission>,
    /// ServiceAccounts within tenant namespaces can be promoted to owners of the given tenant
    /// this can be achieved by labeling the serviceaccount and then they are considered owners. This can only be done by other owners of the tenant.
    /// However ServiceAccounts which have been promoted to owner can not promote further serviceAccounts.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "allowServiceAccountPromotion")]
    pub allow_service_account_promotion: Option<bool>,
    /// Define the period of time upon a cache invalidation is executed for all caches.
    #[serde(rename = "cacheInvalidation")]
    pub cache_invalidation: String,
    /// Toggles the TLS reconciler, the controller that is able to generate CA and certificates for the webhooks
    /// when not using an already provided CA and certificate, or when these are managed externally with Vault, or cert-manager.
    #[serde(rename = "enableTLSReconciler")]
    pub enable_tls_reconciler: bool,
    /// Enforces the Tenant owner, during Namespace creation, to name it using the selected Tenant name as prefix,
    /// separated by a dash. This is useful to avoid Namespace name collision in a public CaaS environment.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "forceTenantPrefix")]
    pub force_tenant_prefix: Option<bool>,
    /// Define groups which when found in the request of a user will be ignored by the Capsule
    /// this might be useful if you have one group where all the users are in, but you want to separate administrators from normal users with additional groups.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "ignoreUserWithGroups")]
    pub ignore_user_with_groups: Option<Vec<String>>,
    /// Service Account Client configuration for impersonation properties
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub impersonation: Option<CapsuleConfigurationImpersonation>,
    /// Allows to set the forbidden metadata for the worker nodes that could be patched by a Tenant.
    /// This applies only if the Tenant has an active NodeSelector, and the Owner have right to patch their nodes.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "nodeMetadata")]
    pub node_metadata: Option<CapsuleConfigurationNodeMetadata>,
    /// Allows to set different name rather than the canonical one for the Capsule configuration objects,
    /// such as webhook secret or configurations.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub overrides: Option<CapsuleConfigurationOverrides>,
    /// Disallow creation of namespaces, whose name matches this regexp
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "protectedNamespaceRegex")]
    pub protected_namespace_regex: Option<String>,
    /// Define Properties for managed ClusterRoles by Capsule
    pub rbac: CapsuleConfigurationRbac,
    /// Deprecated: use users property instead (<https://projectcapsule.dev/docs/operating/setup/configuration/#users)>
    /// 
    /// Names of the groups considered as Capsule users.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "userGroups")]
    pub user_groups: Option<Vec<String>>,
    /// Deprecated: use users property instead (<https://projectcapsule.dev/docs/operating/setup/configuration/#users)>
    /// 
    /// Names of the users considered as Capsule users.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "userNames")]
    pub user_names: Option<Vec<String>>,
    /// Define entities which are considered part of the Capsule construct
    /// Users not mentioned here will be ignored by Capsule
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub users: Option<Vec<CapsuleConfigurationUsers>>,
}

#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub struct CapsuleConfigurationAdministrators {
    /// Kind of entity. Possible values are "User", "Group", and "ServiceAccount"
    pub kind: CapsuleConfigurationAdministratorsKind,
    /// Name of the entity.
    pub name: String,
}

#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum CapsuleConfigurationAdministratorsKind {
    User,
    Group,
    ServiceAccount,
}

/// Configuration for dynamic Validating and Mutating Admission webhooks managed by Capsule.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmission {
    /// Configure dynamic Mutating Admission for Capsule
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mutating: Option<CapsuleConfigurationAdmissionMutating>,
    /// Service Name of the Admission Service
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "serviceName")]
    pub service_name: Option<String>,
    /// Configure dynamic Validating Admission for Capsule
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub validating: Option<CapsuleConfigurationAdmissionValidating>,
}

/// Configure dynamic Mutating Admission for Capsule
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionMutating {
    /// Annotations added to the Admission Webhook
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub annotations: Option<BTreeMap<String, String>>,
    /// whats the problem
    pub client: CapsuleConfigurationAdmissionMutatingClient,
    /// Labels added to the Admission Webhook
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub labels: Option<BTreeMap<String, String>>,
    /// Name the Admission Webhook
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Define Dynamic Admission Webhooks
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub webhooks: Option<Vec<CapsuleConfigurationAdmissionMutatingWebhooks>>,
}

/// whats the problem
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionMutatingClient {
    /// `caBundle` is a PEM encoded CA bundle which will be used to validate the webhook's server certificate.
    /// If unspecified, system trust roots on the apiserver are used.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "caBundle")]
    pub ca_bundle: Option<String>,
    /// `service` is a reference to the service for this webhook. Either
    /// `service` or `url` must be specified.
    /// 
    /// If the webhook is running within the cluster, then you should use `service`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service: Option<CapsuleConfigurationAdmissionMutatingClientService>,
    /// `url` gives the location of the webhook, in standard URL form
    /// (`scheme://host:port/path`). Exactly one of `url` or `service`
    /// must be specified.
    /// 
    /// The `host` should not refer to a service running in the cluster; use
    /// the `service` field instead. The host might be resolved via external
    /// DNS in some apiservers (e.g., `kube-apiserver` cannot resolve
    /// in-cluster DNS as that would be a layering violation). `host` may
    /// also be an IP address.
    /// 
    /// Please note that using `localhost` or `127.0.0.1` as a `host` is
    /// risky unless you take great care to run this webhook on all hosts
    /// which run an apiserver which might need to make calls to this
    /// webhook. Such installs are likely to be non-portable, i.e., not easy
    /// to turn up in a new cluster.
    /// 
    /// The scheme must be "https"; the URL must begin with "<https://".>
    /// 
    /// A path is optional, and if present may be any string permissible in
    /// a URL. You may use the path to pass an arbitrary string to the
    /// webhook, for example, a cluster identifier.
    /// 
    /// Attempting to use a user or basic auth e.g. "user:password@" is not
    /// allowed. Fragments ("#...") and query parameters ("?...") are not
    /// allowed, either.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url: Option<String>,
}

/// `service` is a reference to the service for this webhook. Either
/// `service` or `url` must be specified.
/// 
/// If the webhook is running within the cluster, then you should use `service`.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionMutatingClientService {
    /// `name` is the name of the service.
    /// Required
    pub name: String,
    /// `namespace` is the namespace of the service.
    /// Required
    pub namespace: String,
    /// `path` is an optional URL path which will be sent in any request to
    /// this service.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub path: Option<String>,
    /// If specified, the port on the service that hosting webhook.
    /// Default to 443 for backward compatibility.
    /// `port` should be a valid port number (1-65535, inclusive).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub port: Option<i32>,
}

#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionMutatingWebhooks {
    /// AdmissionReviewVersions is an ordered list of preferred `AdmissionReview`
    /// versions the Webhook expects. API server will try to use first version in
    /// the list which it supports. If none of the versions specified in this list
    /// supported by API server, validation will fail for this object.
    /// If a persisted webhook configuration specifies allowed versions and does not
    /// include any versions known to the API Server, calls to the webhook will fail
    /// and be subject to the failure policy.
    #[serde(rename = "admissionReviewVersions")]
    pub admission_review_versions: Vec<String>,
    /// FailurePolicy defines how unrecognized errors from the admission endpoint are handled -
    /// allowed values are Ignore or Fail. Defaults to Fail.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "failurePolicy")]
    pub failure_policy: Option<String>,
    /// MatchConditions is a list of conditions that must be met for a request to be sent to this
    /// webhook. Match conditions filter requests that have already been matched by the rules,
    /// namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
    /// There are a maximum of 64 match conditions allowed.
    /// 
    /// The exact matching logic is (in order):
    ///   1. If ANY matchCondition evaluates to FALSE, the webhook is skipped.
    ///   2. If ALL matchConditions evaluate to TRUE, the webhook is called.
    ///   3. If any matchCondition evaluates to an error (but none are FALSE):
    ///      - If failurePolicy=Fail, reject the request
    ///      - If failurePolicy=Ignore, the error is ignored and the webhook is skipped
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchConditions")]
    pub match_conditions: Option<Vec<CapsuleConfigurationAdmissionMutatingWebhooksMatchConditions>>,
    /// matchPolicy defines how the "rules" list is used to match incoming requests.
    /// Allowed values are "Exact" or "Equivalent".
    /// 
    /// - Exact: match a request only if it exactly matches a specified rule.
    /// For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
    /// but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
    /// a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the webhook.
    /// 
    /// - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
    /// For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
    /// and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
    /// a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the webhook.
    /// 
    /// Defaults to "Equivalent"
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchPolicy")]
    pub match_policy: Option<String>,
    /// The name of the admission webhook.
    /// Name should be fully qualified, e.g., imagepolicy.kubernetes.io, where
    /// "imagepolicy" is the name of the webhook, and kubernetes.io is the name
    /// of the organization.
    /// Required.
    pub name: String,
    /// NamespaceSelector decides whether to run the webhook on an object based
    /// on whether the namespace for that object matches the selector. If the
    /// object itself is a namespace, the matching is performed on
    /// object.metadata.labels. If the object is another cluster scoped resource,
    /// it never skips the webhook.
    /// 
    /// For example, to run the webhook on any objects whose namespace is not
    /// associated with "runlevel" of "0" or "1";  you will set the selector as
    /// follows:
    /// "namespaceSelector": {
    ///   "matchExpressions": [
    ///     {
    ///       "key": "runlevel",
    ///       "operator": "NotIn",
    ///       "values": [
    ///         "0",
    ///         "1"
    ///       ]
    ///     }
    ///   ]
    /// }
    /// 
    /// If instead you want to only run the webhook on any objects whose
    /// namespace is associated with the "environment" of "prod" or "staging";
    /// you will set the selector as follows:
    /// "namespaceSelector": {
    ///   "matchExpressions": [
    ///     {
    ///       "key": "environment",
    ///       "operator": "In",
    ///       "values": [
    ///         "prod",
    ///         "staging"
    ///       ]
    ///     }
    ///   ]
    /// }
    /// 
    /// See
    /// <https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/>
    /// for more examples of label selectors.
    /// 
    /// Default to the empty LabelSelector, which matches everything.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "namespaceSelector")]
    pub namespace_selector: Option<CapsuleConfigurationAdmissionMutatingWebhooksNamespaceSelector>,
    /// ObjectSelector decides whether to run the webhook based on if the
    /// object has matching labels. objectSelector is evaluated against both
    /// the oldObject and newObject that would be sent to the webhook, and
    /// is considered to match if either object matches the selector. A null
    /// object (oldObject in the case of create, or newObject in the case of
    /// delete) or an object that cannot have labels (like a
    /// DeploymentRollback or a PodProxyOptions object) is not considered to
    /// match.
    /// Use the object selector only if the webhook is opt-in, because end
    /// users may skip the admission webhook by setting the labels.
    /// Default to the empty LabelSelector, which matches everything.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "objectSelector")]
    pub object_selector: Option<CapsuleConfigurationAdmissionMutatingWebhooksObjectSelector>,
    /// Capsule Custom Admission Options
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub opts: Option<CapsuleConfigurationAdmissionMutatingWebhooksOpts>,
    /// `path` is the URL path which will be sent in any request to
    /// this service.
    pub path: String,
    /// reinvocationPolicy indicates whether this webhook should be called multiple times as part of a single admission evaluation.
    /// Allowed values are "Never" and "IfNeeded".
    /// 
    /// Never: the webhook will not be called more than once in a single admission evaluation.
    /// 
    /// IfNeeded: the webhook will be called at least one additional time as part of the admission evaluation
    /// if the object being admitted is modified by other admission plugins after the initial webhook call.
    /// Webhooks that specify this option *must* be idempotent, able to process objects they previously admitted.
    /// Note:
    /// * the number of additional invocations is not guaranteed to be exactly one.
    /// * if additional invocations result in further modifications to the object, webhooks are not guaranteed to be invoked again.
    /// * webhooks that use this option may be reordered to minimize the number of additional invocations.
    /// * to validate an object after all mutations are guaranteed complete, use a validating admission webhook instead.
    /// 
    /// Defaults to "Never".
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "reinvocationPolicy")]
    pub reinvocation_policy: Option<String>,
    /// Rules describes what operations on what resources/subresources the webhook cares about.
    /// The webhook cares about an operation if it matches _any_ Rule.
    /// However, in order to prevent ValidatingAdmissionWebhooks and MutatingAdmissionWebhooks
    /// from putting the cluster in a state which cannot be recovered from without completely
    /// disabling the plugin, ValidatingAdmissionWebhooks and MutatingAdmissionWebhooks are never called
    /// on admission requests for ValidatingWebhookConfiguration and MutatingWebhookConfiguration objects.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub rules: Option<Vec<CapsuleConfigurationAdmissionMutatingWebhooksRules>>,
    /// SideEffects states whether this webhook has side effects.
    /// Acceptable values are: None, NoneOnDryRun (webhooks created via v1beta1 may also specify Some or Unknown).
    /// Webhooks with side effects MUST implement a reconciliation system, since a request may be
    /// rejected by a future step in the admission chain and the side effects therefore need to be undone.
    /// Requests with the dryRun attribute will be auto-rejected if they match a webhook with
    /// sideEffects == Unknown or Some.
    #[serde(rename = "sideEffects")]
    pub side_effects: String,
    /// TimeoutSeconds specifies the timeout for this webhook. After the timeout passes,
    /// the webhook call will be ignored or the API call will fail based on the
    /// failure policy.
    /// The timeout value must be between 1 and 30 seconds.
    /// Default to 10 seconds.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "timeoutSeconds")]
    pub timeout_seconds: Option<i32>,
}

/// MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionMutatingWebhooksMatchConditions {
    /// Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
    /// CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
    /// 
    /// 'object' - The object from the incoming request. The value is null for DELETE requests.
    /// 'oldObject' - The existing object. The value is null for CREATE requests.
    /// 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
    /// 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
    ///   See <https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz>
    /// 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
    ///   request resource.
    /// Documentation on CEL: <https://kubernetes.io/docs/reference/using-api/cel/>
    /// 
    /// Required.
    pub expression: String,
    /// Name is an identifier for this match condition, used for strategic merging of MatchConditions,
    /// as well as providing an identifier for logging purposes. A good name should be descriptive of
    /// the associated expression.
    /// Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
    /// must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
    /// '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
    /// optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
    /// 
    /// Required.
    pub name: String,
}

/// NamespaceSelector decides whether to run the webhook on an object based
/// on whether the namespace for that object matches the selector. If the
/// object itself is a namespace, the matching is performed on
/// object.metadata.labels. If the object is another cluster scoped resource,
/// it never skips the webhook.
/// 
/// For example, to run the webhook on any objects whose namespace is not
/// associated with "runlevel" of "0" or "1";  you will set the selector as
/// follows:
/// "namespaceSelector": {
///   "matchExpressions": [
///     {
///       "key": "runlevel",
///       "operator": "NotIn",
///       "values": [
///         "0",
///         "1"
///       ]
///     }
///   ]
/// }
/// 
/// If instead you want to only run the webhook on any objects whose
/// namespace is associated with the "environment" of "prod" or "staging";
/// you will set the selector as follows:
/// "namespaceSelector": {
///   "matchExpressions": [
///     {
///       "key": "environment",
///       "operator": "In",
///       "values": [
///         "prod",
///         "staging"
///       ]
///     }
///   ]
/// }
/// 
/// See
/// <https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/>
/// for more examples of label selectors.
/// 
/// Default to the empty LabelSelector, which matches everything.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionMutatingWebhooksNamespaceSelector {
    /// matchExpressions is a list of label selector requirements. The requirements are ANDed.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchExpressions")]
    pub match_expressions: Option<Vec<CapsuleConfigurationAdmissionMutatingWebhooksNamespaceSelectorMatchExpressions>>,
    /// matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
    /// map is equivalent to an element of matchExpressions, whose key field is "key", the
    /// operator is "In", and the values array contains only "value". The requirements are ANDed.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchLabels")]
    pub match_labels: Option<BTreeMap<String, String>>,
}

/// A label selector requirement is a selector that contains values, a key, and an operator that
/// relates the key and values.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionMutatingWebhooksNamespaceSelectorMatchExpressions {
    /// key is the label key that the selector applies to.
    pub key: String,
    /// operator represents a key's relationship to a set of values.
    /// Valid operators are In, NotIn, Exists and DoesNotExist.
    pub operator: String,
    /// values is an array of string values. If the operator is In or NotIn,
    /// the values array must be non-empty. If the operator is Exists or DoesNotExist,
    /// the values array must be empty. This array is replaced during a strategic
    /// merge patch.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub values: Option<Vec<String>>,
}

/// ObjectSelector decides whether to run the webhook based on if the
/// object has matching labels. objectSelector is evaluated against both
/// the oldObject and newObject that would be sent to the webhook, and
/// is considered to match if either object matches the selector. A null
/// object (oldObject in the case of create, or newObject in the case of
/// delete) or an object that cannot have labels (like a
/// DeploymentRollback or a PodProxyOptions object) is not considered to
/// match.
/// Use the object selector only if the webhook is opt-in, because end
/// users may skip the admission webhook by setting the labels.
/// Default to the empty LabelSelector, which matches everything.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionMutatingWebhooksObjectSelector {
    /// matchExpressions is a list of label selector requirements. The requirements are ANDed.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchExpressions")]
    pub match_expressions: Option<Vec<CapsuleConfigurationAdmissionMutatingWebhooksObjectSelectorMatchExpressions>>,
    /// matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
    /// map is equivalent to an element of matchExpressions, whose key field is "key", the
    /// operator is "In", and the values array contains only "value". The requirements are ANDed.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchLabels")]
    pub match_labels: Option<BTreeMap<String, String>>,
}

/// A label selector requirement is a selector that contains values, a key, and an operator that
/// relates the key and values.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionMutatingWebhooksObjectSelectorMatchExpressions {
    /// key is the label key that the selector applies to.
    pub key: String,
    /// operator represents a key's relationship to a set of values.
    /// Valid operators are In, NotIn, Exists and DoesNotExist.
    pub operator: String,
    /// values is an array of string values. If the operator is In or NotIn,
    /// the values array must be non-empty. If the operator is Exists or DoesNotExist,
    /// the values array must be empty. This array is replaced during a strategic
    /// merge patch.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub values: Option<Vec<String>>,
}

/// Capsule Custom Admission Options
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionMutatingWebhooksOpts {
    /// If enabled, the request is only sent to admission if the user is mentioned
    /// As Part of the Capsule Administrators
    pub administrators: bool,
    /// If enabled, the request is only sent to admission if the user is mentioned
    /// As Part of the Capsule Users
    #[serde(rename = "capsuleUsers")]
    pub capsule_users: bool,
}

/// RuleWithOperations is a tuple of Operations and Resources. It is recommended to make
/// sure that all the tuple expansions are valid.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionMutatingWebhooksRules {
    /// APIGroups is the API groups the resources belong to. '*' is all groups.
    /// If '*' is present, the length of the slice must be one.
    /// Required.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "apiGroups")]
    pub api_groups: Option<Vec<String>>,
    /// APIVersions is the API versions the resources belong to. '*' is all versions.
    /// If '*' is present, the length of the slice must be one.
    /// Required.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "apiVersions")]
    pub api_versions: Option<Vec<String>>,
    /// Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
    /// for all of those operations and any future admission operations that are added.
    /// If '*' is present, the length of the slice must be one.
    /// Required.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub operations: Option<Vec<String>>,
    /// Resources is a list of resources this rule applies to.
    /// 
    /// For example:
    /// 'pods' means pods.
    /// 'pods/log' means the log subresource of pods.
    /// '*' means all resources, but not subresources.
    /// 'pods/*' means all subresources of pods.
    /// '*/scale' means all scale subresources.
    /// '*/*' means all resources and their subresources.
    /// 
    /// If wildcard is present, the validation rule will ensure resources do not
    /// overlap with each other.
    /// 
    /// Depending on the enclosing object, subresources might not be allowed.
    /// Required.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub resources: Option<Vec<String>>,
    /// scope specifies the scope of this rule.
    /// Valid values are "Cluster", "Namespaced", and "*"
    /// "Cluster" means that only cluster-scoped resources will match this rule.
    /// Namespace API objects are cluster-scoped.
    /// "Namespaced" means that only namespaced resources will match this rule.
    /// "*" means that there are no scope restrictions.
    /// Subresources match the scope of their parent resource.
    /// Default is "*".
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scope: Option<String>,
}

/// Configure dynamic Validating Admission for Capsule
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionValidating {
    /// Annotations added to the Admission Webhook
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub annotations: Option<BTreeMap<String, String>>,
    /// whats the problem
    pub client: CapsuleConfigurationAdmissionValidatingClient,
    /// Labels added to the Admission Webhook
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub labels: Option<BTreeMap<String, String>>,
    /// Name the Admission Webhook
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Define Dynamic Admission Webhooks
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub webhooks: Option<Vec<CapsuleConfigurationAdmissionValidatingWebhooks>>,
}

/// whats the problem
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionValidatingClient {
    /// `caBundle` is a PEM encoded CA bundle which will be used to validate the webhook's server certificate.
    /// If unspecified, system trust roots on the apiserver are used.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "caBundle")]
    pub ca_bundle: Option<String>,
    /// `service` is a reference to the service for this webhook. Either
    /// `service` or `url` must be specified.
    /// 
    /// If the webhook is running within the cluster, then you should use `service`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service: Option<CapsuleConfigurationAdmissionValidatingClientService>,
    /// `url` gives the location of the webhook, in standard URL form
    /// (`scheme://host:port/path`). Exactly one of `url` or `service`
    /// must be specified.
    /// 
    /// The `host` should not refer to a service running in the cluster; use
    /// the `service` field instead. The host might be resolved via external
    /// DNS in some apiservers (e.g., `kube-apiserver` cannot resolve
    /// in-cluster DNS as that would be a layering violation). `host` may
    /// also be an IP address.
    /// 
    /// Please note that using `localhost` or `127.0.0.1` as a `host` is
    /// risky unless you take great care to run this webhook on all hosts
    /// which run an apiserver which might need to make calls to this
    /// webhook. Such installs are likely to be non-portable, i.e., not easy
    /// to turn up in a new cluster.
    /// 
    /// The scheme must be "https"; the URL must begin with "<https://".>
    /// 
    /// A path is optional, and if present may be any string permissible in
    /// a URL. You may use the path to pass an arbitrary string to the
    /// webhook, for example, a cluster identifier.
    /// 
    /// Attempting to use a user or basic auth e.g. "user:password@" is not
    /// allowed. Fragments ("#...") and query parameters ("?...") are not
    /// allowed, either.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url: Option<String>,
}

/// `service` is a reference to the service for this webhook. Either
/// `service` or `url` must be specified.
/// 
/// If the webhook is running within the cluster, then you should use `service`.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionValidatingClientService {
    /// `name` is the name of the service.
    /// Required
    pub name: String,
    /// `namespace` is the namespace of the service.
    /// Required
    pub namespace: String,
    /// `path` is an optional URL path which will be sent in any request to
    /// this service.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub path: Option<String>,
    /// If specified, the port on the service that hosting webhook.
    /// Default to 443 for backward compatibility.
    /// `port` should be a valid port number (1-65535, inclusive).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub port: Option<i32>,
}

#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionValidatingWebhooks {
    /// AdmissionReviewVersions is an ordered list of preferred `AdmissionReview`
    /// versions the Webhook expects. API server will try to use first version in
    /// the list which it supports. If none of the versions specified in this list
    /// supported by API server, validation will fail for this object.
    /// If a persisted webhook configuration specifies allowed versions and does not
    /// include any versions known to the API Server, calls to the webhook will fail
    /// and be subject to the failure policy.
    #[serde(rename = "admissionReviewVersions")]
    pub admission_review_versions: Vec<String>,
    /// FailurePolicy defines how unrecognized errors from the admission endpoint are handled -
    /// allowed values are Ignore or Fail. Defaults to Fail.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "failurePolicy")]
    pub failure_policy: Option<String>,
    /// MatchConditions is a list of conditions that must be met for a request to be sent to this
    /// webhook. Match conditions filter requests that have already been matched by the rules,
    /// namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
    /// There are a maximum of 64 match conditions allowed.
    /// 
    /// The exact matching logic is (in order):
    ///   1. If ANY matchCondition evaluates to FALSE, the webhook is skipped.
    ///   2. If ALL matchConditions evaluate to TRUE, the webhook is called.
    ///   3. If any matchCondition evaluates to an error (but none are FALSE):
    ///      - If failurePolicy=Fail, reject the request
    ///      - If failurePolicy=Ignore, the error is ignored and the webhook is skipped
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchConditions")]
    pub match_conditions: Option<Vec<CapsuleConfigurationAdmissionValidatingWebhooksMatchConditions>>,
    /// matchPolicy defines how the "rules" list is used to match incoming requests.
    /// Allowed values are "Exact" or "Equivalent".
    /// 
    /// - Exact: match a request only if it exactly matches a specified rule.
    /// For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
    /// but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
    /// a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the webhook.
    /// 
    /// - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
    /// For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
    /// and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
    /// a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the webhook.
    /// 
    /// Defaults to "Equivalent"
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchPolicy")]
    pub match_policy: Option<String>,
    /// The name of the admission webhook.
    /// Name should be fully qualified, e.g., imagepolicy.kubernetes.io, where
    /// "imagepolicy" is the name of the webhook, and kubernetes.io is the name
    /// of the organization.
    /// Required.
    pub name: String,
    /// NamespaceSelector decides whether to run the webhook on an object based
    /// on whether the namespace for that object matches the selector. If the
    /// object itself is a namespace, the matching is performed on
    /// object.metadata.labels. If the object is another cluster scoped resource,
    /// it never skips the webhook.
    /// 
    /// For example, to run the webhook on any objects whose namespace is not
    /// associated with "runlevel" of "0" or "1";  you will set the selector as
    /// follows:
    /// "namespaceSelector": {
    ///   "matchExpressions": [
    ///     {
    ///       "key": "runlevel",
    ///       "operator": "NotIn",
    ///       "values": [
    ///         "0",
    ///         "1"
    ///       ]
    ///     }
    ///   ]
    /// }
    /// 
    /// If instead you want to only run the webhook on any objects whose
    /// namespace is associated with the "environment" of "prod" or "staging";
    /// you will set the selector as follows:
    /// "namespaceSelector": {
    ///   "matchExpressions": [
    ///     {
    ///       "key": "environment",
    ///       "operator": "In",
    ///       "values": [
    ///         "prod",
    ///         "staging"
    ///       ]
    ///     }
    ///   ]
    /// }
    /// 
    /// See
    /// <https://kubernetes.io/docs/concepts/overview/working-with-objects/labels>
    /// for more examples of label selectors.
    /// 
    /// Default to the empty LabelSelector, which matches everything.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "namespaceSelector")]
    pub namespace_selector: Option<CapsuleConfigurationAdmissionValidatingWebhooksNamespaceSelector>,
    /// ObjectSelector decides whether to run the webhook based on if the
    /// object has matching labels. objectSelector is evaluated against both
    /// the oldObject and newObject that would be sent to the webhook, and
    /// is considered to match if either object matches the selector. A null
    /// object (oldObject in the case of create, or newObject in the case of
    /// delete) or an object that cannot have labels (like a
    /// DeploymentRollback or a PodProxyOptions object) is not considered to
    /// match.
    /// Use the object selector only if the webhook is opt-in, because end
    /// users may skip the admission webhook by setting the labels.
    /// Default to the empty LabelSelector, which matches everything.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "objectSelector")]
    pub object_selector: Option<CapsuleConfigurationAdmissionValidatingWebhooksObjectSelector>,
    /// Capsule Custom Admission Options
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub opts: Option<CapsuleConfigurationAdmissionValidatingWebhooksOpts>,
    /// `path` is the URL path which will be sent in any request to
    /// this service.
    pub path: String,
    /// Rules describes what operations on what resources/subresources the webhook cares about.
    /// The webhook cares about an operation if it matches _any_ Rule.
    /// However, in order to prevent ValidatingAdmissionWebhooks and MutatingAdmissionWebhooks
    /// from putting the cluster in a state which cannot be recovered from without completely
    /// disabling the plugin, ValidatingAdmissionWebhooks and MutatingAdmissionWebhooks are never called
    /// on admission requests for ValidatingWebhookConfiguration and MutatingWebhookConfiguration objects.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub rules: Option<Vec<CapsuleConfigurationAdmissionValidatingWebhooksRules>>,
    /// SideEffects states whether this webhook has side effects.
    /// Acceptable values are: None, NoneOnDryRun (webhooks created via v1beta1 may also specify Some or Unknown).
    /// Webhooks with side effects MUST implement a reconciliation system, since a request may be
    /// rejected by a future step in the admission chain and the side effects therefore need to be undone.
    /// Requests with the dryRun attribute will be auto-rejected if they match a webhook with
    /// sideEffects == Unknown or Some.
    #[serde(rename = "sideEffects")]
    pub side_effects: String,
    /// TimeoutSeconds specifies the timeout for this webhook. After the timeout passes,
    /// the webhook call will be ignored or the API call will fail based on the
    /// failure policy.
    /// The timeout value must be between 1 and 30 seconds.
    /// Default to 10 seconds.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "timeoutSeconds")]
    pub timeout_seconds: Option<i32>,
}

/// MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionValidatingWebhooksMatchConditions {
    /// Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
    /// CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
    /// 
    /// 'object' - The object from the incoming request. The value is null for DELETE requests.
    /// 'oldObject' - The existing object. The value is null for CREATE requests.
    /// 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
    /// 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
    ///   See <https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz>
    /// 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
    ///   request resource.
    /// Documentation on CEL: <https://kubernetes.io/docs/reference/using-api/cel/>
    /// 
    /// Required.
    pub expression: String,
    /// Name is an identifier for this match condition, used for strategic merging of MatchConditions,
    /// as well as providing an identifier for logging purposes. A good name should be descriptive of
    /// the associated expression.
    /// Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
    /// must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
    /// '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
    /// optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
    /// 
    /// Required.
    pub name: String,
}

/// NamespaceSelector decides whether to run the webhook on an object based
/// on whether the namespace for that object matches the selector. If the
/// object itself is a namespace, the matching is performed on
/// object.metadata.labels. If the object is another cluster scoped resource,
/// it never skips the webhook.
/// 
/// For example, to run the webhook on any objects whose namespace is not
/// associated with "runlevel" of "0" or "1";  you will set the selector as
/// follows:
/// "namespaceSelector": {
///   "matchExpressions": [
///     {
///       "key": "runlevel",
///       "operator": "NotIn",
///       "values": [
///         "0",
///         "1"
///       ]
///     }
///   ]
/// }
/// 
/// If instead you want to only run the webhook on any objects whose
/// namespace is associated with the "environment" of "prod" or "staging";
/// you will set the selector as follows:
/// "namespaceSelector": {
///   "matchExpressions": [
///     {
///       "key": "environment",
///       "operator": "In",
///       "values": [
///         "prod",
///         "staging"
///       ]
///     }
///   ]
/// }
/// 
/// See
/// <https://kubernetes.io/docs/concepts/overview/working-with-objects/labels>
/// for more examples of label selectors.
/// 
/// Default to the empty LabelSelector, which matches everything.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionValidatingWebhooksNamespaceSelector {
    /// matchExpressions is a list of label selector requirements. The requirements are ANDed.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchExpressions")]
    pub match_expressions: Option<Vec<CapsuleConfigurationAdmissionValidatingWebhooksNamespaceSelectorMatchExpressions>>,
    /// matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
    /// map is equivalent to an element of matchExpressions, whose key field is "key", the
    /// operator is "In", and the values array contains only "value". The requirements are ANDed.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchLabels")]
    pub match_labels: Option<BTreeMap<String, String>>,
}

/// A label selector requirement is a selector that contains values, a key, and an operator that
/// relates the key and values.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionValidatingWebhooksNamespaceSelectorMatchExpressions {
    /// key is the label key that the selector applies to.
    pub key: String,
    /// operator represents a key's relationship to a set of values.
    /// Valid operators are In, NotIn, Exists and DoesNotExist.
    pub operator: String,
    /// values is an array of string values. If the operator is In or NotIn,
    /// the values array must be non-empty. If the operator is Exists or DoesNotExist,
    /// the values array must be empty. This array is replaced during a strategic
    /// merge patch.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub values: Option<Vec<String>>,
}

/// ObjectSelector decides whether to run the webhook based on if the
/// object has matching labels. objectSelector is evaluated against both
/// the oldObject and newObject that would be sent to the webhook, and
/// is considered to match if either object matches the selector. A null
/// object (oldObject in the case of create, or newObject in the case of
/// delete) or an object that cannot have labels (like a
/// DeploymentRollback or a PodProxyOptions object) is not considered to
/// match.
/// Use the object selector only if the webhook is opt-in, because end
/// users may skip the admission webhook by setting the labels.
/// Default to the empty LabelSelector, which matches everything.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionValidatingWebhooksObjectSelector {
    /// matchExpressions is a list of label selector requirements. The requirements are ANDed.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchExpressions")]
    pub match_expressions: Option<Vec<CapsuleConfigurationAdmissionValidatingWebhooksObjectSelectorMatchExpressions>>,
    /// matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
    /// map is equivalent to an element of matchExpressions, whose key field is "key", the
    /// operator is "In", and the values array contains only "value". The requirements are ANDed.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchLabels")]
    pub match_labels: Option<BTreeMap<String, String>>,
}

/// A label selector requirement is a selector that contains values, a key, and an operator that
/// relates the key and values.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionValidatingWebhooksObjectSelectorMatchExpressions {
    /// key is the label key that the selector applies to.
    pub key: String,
    /// operator represents a key's relationship to a set of values.
    /// Valid operators are In, NotIn, Exists and DoesNotExist.
    pub operator: String,
    /// values is an array of string values. If the operator is In or NotIn,
    /// the values array must be non-empty. If the operator is Exists or DoesNotExist,
    /// the values array must be empty. This array is replaced during a strategic
    /// merge patch.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub values: Option<Vec<String>>,
}

/// Capsule Custom Admission Options
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionValidatingWebhooksOpts {
    /// If enabled, the request is only sent to admission if the user is mentioned
    /// As Part of the Capsule Administrators
    pub administrators: bool,
    /// If enabled, the request is only sent to admission if the user is mentioned
    /// As Part of the Capsule Users
    #[serde(rename = "capsuleUsers")]
    pub capsule_users: bool,
}

/// RuleWithOperations is a tuple of Operations and Resources. It is recommended to make
/// sure that all the tuple expansions are valid.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationAdmissionValidatingWebhooksRules {
    /// APIGroups is the API groups the resources belong to. '*' is all groups.
    /// If '*' is present, the length of the slice must be one.
    /// Required.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "apiGroups")]
    pub api_groups: Option<Vec<String>>,
    /// APIVersions is the API versions the resources belong to. '*' is all versions.
    /// If '*' is present, the length of the slice must be one.
    /// Required.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "apiVersions")]
    pub api_versions: Option<Vec<String>>,
    /// Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
    /// for all of those operations and any future admission operations that are added.
    /// If '*' is present, the length of the slice must be one.
    /// Required.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub operations: Option<Vec<String>>,
    /// Resources is a list of resources this rule applies to.
    /// 
    /// For example:
    /// 'pods' means pods.
    /// 'pods/log' means the log subresource of pods.
    /// '*' means all resources, but not subresources.
    /// 'pods/*' means all subresources of pods.
    /// '*/scale' means all scale subresources.
    /// '*/*' means all resources and their subresources.
    /// 
    /// If wildcard is present, the validation rule will ensure resources do not
    /// overlap with each other.
    /// 
    /// Depending on the enclosing object, subresources might not be allowed.
    /// Required.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub resources: Option<Vec<String>>,
    /// scope specifies the scope of this rule.
    /// Valid values are "Cluster", "Namespaced", and "*"
    /// "Cluster" means that only cluster-scoped resources will match this rule.
    /// Namespace API objects are cluster-scoped.
    /// "Namespaced" means that only namespaced resources will match this rule.
    /// "*" means that there are no scope restrictions.
    /// Subresources match the scope of their parent resource.
    /// Default is "*".
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scope: Option<String>,
}

/// Service Account Client configuration for impersonation properties
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationImpersonation {
    /// Key in the secret that holds the CA certificate (e.g., "ca.crt")
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "caSecretKey")]
    pub ca_secret_key: Option<String>,
    /// Name of the secret containing the CA certificate
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "caSecretName")]
    pub ca_secret_name: Option<String>,
    /// Namespace where the CA certificate secret is located
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "caSecretNamespace")]
    pub ca_secret_namespace: Option<String>,
    /// Kubernetes API Endpoint to use for impersonation
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint: Option<String>,
    /// Default ServiceAccount for global resources (GlobalTenantResource)
    /// When defined, users are required to use this ServiceAccount anywhere in the cluster
    /// unless they explicitly provide their own.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "globalDefaultServiceAccount")]
    pub global_default_service_account: Option<String>,
    /// Default ServiceAccount for global resources (GlobalTenantResource)
    /// When defined, users are required to use this ServiceAccount anywhere in the cluster
    /// unless they explicitly provide their own.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "globalDefaultServiceAccountNamespace")]
    pub global_default_service_account_namespace: Option<String>,
    /// If true, TLS certificate verification is skipped (not recommended for production)
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "skipTlsVerify")]
    pub skip_tls_verify: Option<bool>,
    /// Default ServiceAccount for namespaced resources (TenantResource)
    /// When defined, users are required to use this ServiceAccount within the namespace
    /// where they deploy the resource, unless they explicitly provide their own.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "tenantDefaultServiceAccount")]
    pub tenant_default_service_account: Option<String>,
}

/// Allows to set the forbidden metadata for the worker nodes that could be patched by a Tenant.
/// This applies only if the Tenant has an active NodeSelector, and the Owner have right to patch their nodes.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationNodeMetadata {
    /// Define the annotations that a Tenant Owner cannot set for their nodes.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "forbiddenAnnotations")]
    pub forbidden_annotations: Option<CapsuleConfigurationNodeMetadataForbiddenAnnotations>,
    /// Define the labels that a Tenant Owner cannot set for their nodes.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "forbiddenLabels")]
    pub forbidden_labels: Option<CapsuleConfigurationNodeMetadataForbiddenLabels>,
}

/// Define the annotations that a Tenant Owner cannot set for their nodes.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationNodeMetadataForbiddenAnnotations {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub denied: Option<Vec<String>>,
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "deniedRegex")]
    pub denied_regex: Option<String>,
}

/// Define the labels that a Tenant Owner cannot set for their nodes.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationNodeMetadataForbiddenLabels {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub denied: Option<Vec<String>>,
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "deniedRegex")]
    pub denied_regex: Option<String>,
}

/// Allows to set different name rather than the canonical one for the Capsule configuration objects,
/// such as webhook secret or configurations.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationOverrides {
    /// Defines the Secret name used for the webhook server.
    /// Must be in the same Namespace where the Capsule Deployment is deployed.
    #[serde(rename = "TLSSecretName")]
    pub tls_secret_name: String,
    /// Deprecated: use dynamic admission instead
    /// 
    /// Name of the MutatingWebhookConfiguration which contains the dynamic admission controller paths and resources.
    #[serde(rename = "mutatingWebhookConfigurationName")]
    pub mutating_webhook_configuration_name: String,
    /// Deprecated: use dynamic admission instead
    /// 
    /// Name of the ValidatingWebhookConfiguration which contains the dynamic admission controller paths and resources.
    #[serde(rename = "validatingWebhookConfigurationName")]
    pub validating_webhook_configuration_name: String,
}

/// Define Properties for managed ClusterRoles by Capsule
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationRbac {
    /// The ClusterRoles applied for Administrators
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "administrationClusterRoles")]
    pub administration_cluster_roles: Option<Vec<String>>,
    /// Name for the ClusterRole required to grant Namespace Deletion permissions.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deleter: Option<String>,
    /// The ClusterRoles applied for ServiceAccounts which had owner Promotion
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "promotionClusterRoles")]
    pub promotion_cluster_roles: Option<Vec<String>>,
    /// Name for the ClusterRole required to grant Namespace Provision permissions.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub provisioner: Option<String>,
}

#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub struct CapsuleConfigurationUsers {
    /// Kind of entity. Possible values are "User", "Group", and "ServiceAccount"
    pub kind: CapsuleConfigurationUsersKind,
    /// Name of the entity.
    pub name: String,
}

#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum CapsuleConfigurationUsersKind {
    User,
    Group,
    ServiceAccount,
}

/// CapsuleConfigurationStatus defines the Capsule configuration status.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct CapsuleConfigurationStatus {
    /// Users which are considered Capsule Users and are bound to the Capsule Tenant construct.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub users: Option<Vec<CapsuleConfigurationStatusUsers>>,
}

#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub struct CapsuleConfigurationStatusUsers {
    /// Kind of entity. Possible values are "User", "Group", and "ServiceAccount"
    pub kind: CapsuleConfigurationStatusUsersKind,
    /// Name of the entity.
    pub name: String,
}

#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum CapsuleConfigurationStatusUsersKind {
    User,
    Group,
    ServiceAccount,
}

