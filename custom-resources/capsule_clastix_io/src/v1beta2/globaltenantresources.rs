// WARNING: generated by kopium - manual changes will be overwritten
// kopium command: kopium --docs --derive=Default --derive=PartialEq --smart-derive-elision --filename crd-catalog/projectcapsule/capsule/capsule.clastix.io/v1beta2/globaltenantresources.yaml
// kopium version: 0.23.0

#[allow(unused_imports)]
mod prelude {
    pub use kube::CustomResource;
    pub use serde::{Serialize, Deserialize};
    pub use std::collections::BTreeMap;
    pub use k8s_openapi::apimachinery::pkg::apis::meta::v1::Condition;
}

use self::prelude::*;

/// GlobalTenantResourceSpec defines the desired state of GlobalTenantResource.
#[derive(CustomResource, Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
#[kube(group = "capsule.clastix.io", version = "v1beta2", kind = "GlobalTenantResource", plural = "globaltenantresources")]
#[kube(status = "GlobalTenantResourceStatus")]
#[kube(schema = "disabled")]
#[kube(derive="Default")]
#[kube(derive="PartialEq")]
pub struct GlobalTenantResourceSpec {
    /// When cordoning a replication it will no longer execute any applies or deletions (paused).
    /// This is useful for maintenances
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cordoned: Option<bool>,
    /// DependsOn may contain a meta.NamespacedObjectReference slice
    /// with references to TenantResource resources that must be ready before this
    /// TenantResource can be reconciled.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "dependsOn")]
    pub depends_on: Option<Vec<GlobalTenantResourceDependsOn>>,
    /// When the replicated resource manifest is deleted, all the objects replicated so far will be automatically deleted.
    /// Disable this to keep replicated resources although the deletion of the replication manifest.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "pruningOnDelete")]
    pub pruning_on_delete: Option<bool>,
    /// Defines the rules to select targeting Namespace, along with the objects that must be replicated.
    pub resources: Vec<GlobalTenantResourceResources>,
    /// Define the period of time upon a second reconciliation must be invoked.
    /// Keep in mind that any change to the manifests will trigger a new reconciliation.
    #[serde(rename = "resyncPeriod")]
    pub resync_period: String,
    /// Resource Scope, Can either be
    /// - Tenant: Create Resources for each tenant  in selected Tenants
    /// - Namespace: Create Resources for each namespace in selected Tenants
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scope: Option<GlobalTenantResourceScope>,
    /// Local ServiceAccount which will perform all the actions defined in the TenantResource
    /// You must provide permissions accordingly to that ServiceAccount
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "serviceAccount")]
    pub service_account: Option<GlobalTenantResourceServiceAccount>,
    /// Provide additional settings
    pub settings: GlobalTenantResourceSettings,
    /// Defines the Tenant selector used target the tenants on which resources must be propagated.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "tenantSelector")]
    pub tenant_selector: Option<GlobalTenantResourceTenantSelector>,
}

/// LocalObjectReference contains enough information to locate the referenced Kubernetes resource object.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceDependsOn {
    /// Name of the referent.
    pub name: String,
}

#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceResources {
    /// Besides the Capsule metadata required by TenantResource controller, defines additional metadata that must be
    /// added to the replicated resources.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "additionalMetadata")]
    pub additional_metadata: Option<GlobalTenantResourceResourcesAdditionalMetadata>,
    /// Provide additional template context, which can be used throughout all
    /// the declared items for the replication
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub context: Option<GlobalTenantResourceResourcesContext>,
    /// Templates for advanced use cases
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub generators: Option<Vec<GlobalTenantResourceResourcesGenerators>>,
    /// Defines the Namespace selector to select the Tenant Namespaces on which the resources must be propagated.
    /// In case of nil value, all the Tenant Namespaces are targeted.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "namespaceSelector")]
    pub namespace_selector: Option<GlobalTenantResourceResourcesNamespaceSelector>,
    /// List of the resources already existing in other Namespaces that must be replicated.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "namespacedItems")]
    pub namespaced_items: Option<Vec<GlobalTenantResourceResourcesNamespacedItems>>,
    /// List of raw resources that must be replicated.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "rawItems")]
    pub raw_items: Option<Vec<BTreeMap<String, serde_json::Value>>>,
}

/// Besides the Capsule metadata required by TenantResource controller, defines additional metadata that must be
/// added to the replicated resources.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceResourcesAdditionalMetadata {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub annotations: Option<BTreeMap<String, String>>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub labels: Option<BTreeMap<String, String>>,
}

/// Provide additional template context, which can be used throughout all
/// the declared items for the replication
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceResourcesContext {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub resources: Option<Vec<GlobalTenantResourceResourcesContextResources>>,
}

#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceResourcesContextResources {
    /// API version of the referent.
    #[serde(rename = "apiVersion")]
    pub api_version: String,
    /// Index to mount the resource in the template context
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub index: Option<String>,
    /// Kind of the referent.
    /// More info: <https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds>
    pub kind: String,
    /// Name of the values referent. This is useful
    /// when you traying to get a specific resource
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Namespace of the values referent.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub namespace: Option<String>,
    /// Only relevant if name is set. If an item is not optional, there will be an error thrown when it does not exist
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub optional: Option<bool>,
    /// Selector which allows to get any amount of these resources based on labels
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub selector: Option<GlobalTenantResourceResourcesContextResourcesSelector>,
}

/// Selector which allows to get any amount of these resources based on labels
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceResourcesContextResourcesSelector {
    /// matchExpressions is a list of label selector requirements. The requirements are ANDed.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchExpressions")]
    pub match_expressions: Option<Vec<GlobalTenantResourceResourcesContextResourcesSelectorMatchExpressions>>,
    /// matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
    /// map is equivalent to an element of matchExpressions, whose key field is "key", the
    /// operator is "In", and the values array contains only "value". The requirements are ANDed.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchLabels")]
    pub match_labels: Option<BTreeMap<String, String>>,
}

/// A label selector requirement is a selector that contains values, a key, and an operator that
/// relates the key and values.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceResourcesContextResourcesSelectorMatchExpressions {
    /// key is the label key that the selector applies to.
    pub key: String,
    /// operator represents a key's relationship to a set of values.
    /// Valid operators are In, NotIn, Exists and DoesNotExist.
    pub operator: String,
    /// values is an array of string values. If the operator is In or NotIn,
    /// the values array must be non-empty. If the operator is Exists or DoesNotExist,
    /// the values array must be empty. This array is replaced during a strategic
    /// merge patch.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub values: Option<Vec<String>>,
}

#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceResourcesGenerators {
    /// Missing Key Option for templating
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "missingKey")]
    pub missing_key: Option<GlobalTenantResourceResourcesGeneratorsMissingKey>,
    /// Template contains any amount of yaml which is applied to Kubernetes.
    /// This can be a single resource or multiple resources
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub template: Option<String>,
}

#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum GlobalTenantResourceResourcesGeneratorsMissingKey {
    #[serde(rename = "invalid")]
    Invalid,
    #[serde(rename = "zero")]
    Zero,
    #[serde(rename = "error")]
    Error,
}

/// Defines the Namespace selector to select the Tenant Namespaces on which the resources must be propagated.
/// In case of nil value, all the Tenant Namespaces are targeted.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceResourcesNamespaceSelector {
    /// matchExpressions is a list of label selector requirements. The requirements are ANDed.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchExpressions")]
    pub match_expressions: Option<Vec<GlobalTenantResourceResourcesNamespaceSelectorMatchExpressions>>,
    /// matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
    /// map is equivalent to an element of matchExpressions, whose key field is "key", the
    /// operator is "In", and the values array contains only "value". The requirements are ANDed.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchLabels")]
    pub match_labels: Option<BTreeMap<String, String>>,
}

/// A label selector requirement is a selector that contains values, a key, and an operator that
/// relates the key and values.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceResourcesNamespaceSelectorMatchExpressions {
    /// key is the label key that the selector applies to.
    pub key: String,
    /// operator represents a key's relationship to a set of values.
    /// Valid operators are In, NotIn, Exists and DoesNotExist.
    pub operator: String,
    /// values is an array of string values. If the operator is In or NotIn,
    /// the values array must be non-empty. If the operator is Exists or DoesNotExist,
    /// the values array must be empty. This array is replaced during a strategic
    /// merge patch.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub values: Option<Vec<String>>,
}

/// Reference
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceResourcesNamespacedItems {
    /// API version of the referent.
    #[serde(rename = "apiVersion")]
    pub api_version: String,
    /// Kind of the referent.
    /// More info: <https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds>
    pub kind: String,
    /// Name of the values referent. This is useful
    /// when you traying to get a specific resource
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Namespace of the values referent.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub namespace: Option<String>,
    /// Only relevant if name is set. If an item is not optional, there will be an error thrown when it does not exist
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub optional: Option<bool>,
    /// Selector which allows to get any amount of these resources based on labels
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub selector: Option<GlobalTenantResourceResourcesNamespacedItemsSelector>,
}

/// Selector which allows to get any amount of these resources based on labels
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceResourcesNamespacedItemsSelector {
    /// matchExpressions is a list of label selector requirements. The requirements are ANDed.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchExpressions")]
    pub match_expressions: Option<Vec<GlobalTenantResourceResourcesNamespacedItemsSelectorMatchExpressions>>,
    /// matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
    /// map is equivalent to an element of matchExpressions, whose key field is "key", the
    /// operator is "In", and the values array contains only "value". The requirements are ANDed.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchLabels")]
    pub match_labels: Option<BTreeMap<String, String>>,
}

/// A label selector requirement is a selector that contains values, a key, and an operator that
/// relates the key and values.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceResourcesNamespacedItemsSelectorMatchExpressions {
    /// key is the label key that the selector applies to.
    pub key: String,
    /// operator represents a key's relationship to a set of values.
    /// Valid operators are In, NotIn, Exists and DoesNotExist.
    pub operator: String,
    /// values is an array of string values. If the operator is In or NotIn,
    /// the values array must be non-empty. If the operator is Exists or DoesNotExist,
    /// the values array must be empty. This array is replaced during a strategic
    /// merge patch.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub values: Option<Vec<String>>,
}

/// GlobalTenantResourceSpec defines the desired state of GlobalTenantResource.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum GlobalTenantResourceScope {
    Namespace,
    Tenant,
    None,
}

/// Local ServiceAccount which will perform all the actions defined in the TenantResource
/// You must provide permissions accordingly to that ServiceAccount
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceServiceAccount {
    /// Name of the referent.
    pub name: String,
    /// Namespace of the referent.
    pub namespace: String,
}

/// Provide additional settings
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceSettings {
    /// Enabling this allows TenanResources to interact with objects which were not created by a TenantResource. In this case on prune no deletion of the entire object is made.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub adopt: Option<bool>,
    /// Force indicates that in case of conflicts with server-side apply, the client should acquire ownership of the conflicting field.
    /// You may create collisions with this.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub force: Option<bool>,
}

/// Defines the Tenant selector used target the tenants on which resources must be propagated.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceTenantSelector {
    /// matchExpressions is a list of label selector requirements. The requirements are ANDed.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchExpressions")]
    pub match_expressions: Option<Vec<GlobalTenantResourceTenantSelectorMatchExpressions>>,
    /// matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
    /// map is equivalent to an element of matchExpressions, whose key field is "key", the
    /// operator is "In", and the values array contains only "value". The requirements are ANDed.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "matchLabels")]
    pub match_labels: Option<BTreeMap<String, String>>,
}

/// A label selector requirement is a selector that contains values, a key, and an operator that
/// relates the key and values.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceTenantSelectorMatchExpressions {
    /// key is the label key that the selector applies to.
    pub key: String,
    /// operator represents a key's relationship to a set of values.
    /// Valid operators are In, NotIn, Exists and DoesNotExist.
    pub operator: String,
    /// values is an array of string values. If the operator is In or NotIn,
    /// the values array must be non-empty. If the operator is Exists or DoesNotExist,
    /// the values array must be empty. This array is replaced during a strategic
    /// merge patch.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub values: Option<Vec<String>>,
}

/// GlobalTenantResourceStatus defines the observed state of GlobalTenantResource.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceStatus {
    /// Condition of the GlobalTenantResource.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub conditions: Option<Vec<Condition>>,
    /// List of the replicated resources for the given TenantResource.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "processedItems")]
    pub processed_items: Option<Vec<GlobalTenantResourceStatusProcessedItems>>,
    /// List of Tenants addressed by the GlobalTenantResource.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "selectedTenants")]
    pub selected_tenants: Option<Vec<String>>,
    /// Serviceaccount used for impersonation
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "serviceAccount")]
    pub service_account: Option<GlobalTenantResourceStatusServiceAccount>,
    /// How many items are being replicated by the TenantResource.
    pub size: i64,
}

/// Advanced Status Item for pin pointing items in tenants/namespaces.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceStatusProcessedItems {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub namespace: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub origin: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<GlobalTenantResourceStatusProcessedItemsStatus>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tenant: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub version: Option<String>,
}

#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub struct GlobalTenantResourceStatusProcessedItemsStatus {
    /// Indicates wether the resource was created or adopted
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created: Option<bool>,
    /// An opaque value that represents the internal version of this object that can
    /// be used by clients to determine when objects have changed. May be used for optimistic
    /// concurrency, change detection, and the watch operation on a resource or set of resources.
    /// Clients must treat these values as opaque and passed unmodified back to the server.
    /// They may only be valid for a particular resource or set of resources.
    /// 
    /// Populated by the system.
    /// Read-only.
    /// Value must be treated as opaque by clients and .
    /// More info: <https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency>
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "lastApply")]
    pub last_apply: Option<String>,
    /// message is a human readable message indicating details about the transition.
    /// This may be an empty string.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
    /// status of the condition, one of True, False, Unknown.
    pub status: GlobalTenantResourceStatusProcessedItemsStatusStatus,
    /// type of condition in CamelCase or in foo.example.com/CamelCase.
    #[serde(rename = "type")]
    pub r#type: String,
}

#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum GlobalTenantResourceStatusProcessedItemsStatusStatus {
    True,
    False,
    Unknown,
}

/// Serviceaccount used for impersonation
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct GlobalTenantResourceStatusServiceAccount {
    /// Name of the referent.
    pub name: String,
    /// Namespace of the referent.
    pub namespace: String,
}

