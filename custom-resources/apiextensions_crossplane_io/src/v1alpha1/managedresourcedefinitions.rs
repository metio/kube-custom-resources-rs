// WARNING: generated by kopium - manual changes will be overwritten
// kopium command: kopium --docs --derive=Default --derive=PartialEq --smart-derive-elision --filename crd-catalog/crossplane/crossplane/apiextensions.crossplane.io/v1alpha1/managedresourcedefinitions.yaml
// kopium version: 0.23.0

#[allow(unused_imports)]
mod prelude {
    pub use kube::CustomResource;
    pub use serde::{Serialize, Deserialize};
    pub use std::collections::BTreeMap;
    pub use k8s_openapi::apimachinery::pkg::apis::meta::v1::Condition;
}

use self::prelude::*;

/// ManagedResourceDefinitionSpec specifies the desired state of the resource definition.
#[derive(CustomResource, Serialize, Deserialize, Clone, Debug, PartialEq)]
#[kube(group = "apiextensions.crossplane.io", version = "v1alpha1", kind = "ManagedResourceDefinition", plural = "managedresourcedefinitions")]
#[kube(status = "ManagedResourceDefinitionStatus")]
#[kube(schema = "disabled")]
#[kube(derive="PartialEq")]
pub struct ManagedResourceDefinitionSpec {
    /// ConnectionDetails is an array of connection detail keys and descriptions.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "connectionDetails")]
    pub connection_details: Option<Vec<ManagedResourceDefinitionConnectionDetails>>,
    /// Conversion defines conversion settings for the CRD.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub conversion: Option<ManagedResourceDefinitionConversion>,
    /// Group is the API group of the defined custom resource.
    /// The custom resources are served under `/apis/<group>/...`.
    /// Must match the name of the CustomResourceDefinition (in the form `<names.plural>.<group>`).
    pub group: String,
    /// Names specify the resource and kind names for the custom resource.
    pub names: ManagedResourceDefinitionNames,
    /// PreserveUnknownFields indicates that object fields which are not specified
    /// in the OpenAPI schema should be preserved when persisting to storage.
    /// apiVersion, kind, metadata and known fields inside metadata are always preserved.
    /// This field is deprecated in favor of setting `x-preserve-unknown-fields` to true in `spec.versions[*].schema.openAPIV3Schema`.
    /// See <https://kubernetes.io/docs/tasks/extend-kubernetes/custom-resources/custom-resource-definitions/#field-pruning> for details.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "preserveUnknownFields")]
    pub preserve_unknown_fields: Option<bool>,
    /// Scope indicates whether the defined custom resource is cluster- or namespace-scoped.
    /// Allowed values are `Cluster` and `Namespaced`.
    pub scope: ManagedResourceDefinitionScope,
    /// State toggles whether the underlying CRD is created or not.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<ManagedResourceDefinitionState>,
    /// Versions is the list of all API versions of the defined custom resource.
    /// Version names are used to compute the order in which served versions are listed in API discovery.
    /// If the version string is "kube-like", it will sort above non "kube-like" version strings, which are ordered
    /// lexicographically. "Kube-like" versions start with a "v", then are followed by a number (the major version),
    /// then optionally the string "alpha" or "beta" and another number (the minor version). These are sorted first
    /// by GA > beta > alpha (where GA is a version with no suffix such as beta or alpha), and then by comparing
    /// major version, then minor version. An example sorted list of versions:
    /// v10, v2, v1, v11beta2, v10beta3, v3beta1, v12alpha1, v11alpha2, foo1, foo10.
    pub versions: Vec<ManagedResourceDefinitionVersions>,
}

/// ConnectionDetail holds keys and descriptions of connection secrets.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct ManagedResourceDefinitionConnectionDetails {
    /// Description of how the key is used.
    pub description: String,
    /// Name of the key.
    pub name: String,
}

/// Conversion defines conversion settings for the CRD.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct ManagedResourceDefinitionConversion {
    /// strategy specifies how custom resources are converted between versions. Allowed values are:
    /// - `"None"`: The converter only change the apiVersion and would not touch any other field in the custom resource.
    /// - `"Webhook"`: API Server will call to an external webhook to do the conversion. Additional information
    ///   is needed for this option. This requires spec.preserveUnknownFields to be false, and spec.conversion.webhook to be set.
    pub strategy: String,
    /// webhook describes how to call the conversion webhook. Required when `strategy` is set to `"Webhook"`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub webhook: Option<ManagedResourceDefinitionConversionWebhook>,
}

/// webhook describes how to call the conversion webhook. Required when `strategy` is set to `"Webhook"`.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct ManagedResourceDefinitionConversionWebhook {
    /// clientConfig is the instructions for how to call the webhook if strategy is `Webhook`.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "clientConfig")]
    pub client_config: Option<ManagedResourceDefinitionConversionWebhookClientConfig>,
    /// conversionReviewVersions is an ordered list of preferred `ConversionReview`
    /// versions the Webhook expects. The API server will use the first version in
    /// the list which it supports. If none of the versions specified in this list
    /// are supported by API server, conversion will fail for the custom resource.
    /// If a persisted Webhook configuration specifies allowed versions and does not
    /// include any versions known to the API Server, calls to the webhook will fail.
    #[serde(rename = "conversionReviewVersions")]
    pub conversion_review_versions: Vec<String>,
}

/// clientConfig is the instructions for how to call the webhook if strategy is `Webhook`.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct ManagedResourceDefinitionConversionWebhookClientConfig {
    /// caBundle is a PEM encoded CA bundle which will be used to validate the webhook's server certificate.
    /// If unspecified, system trust roots on the apiserver are used.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "caBundle")]
    pub ca_bundle: Option<String>,
    /// service is a reference to the service for this webhook. Either
    /// service or url must be specified.
    /// 
    /// If the webhook is running within the cluster, then you should use `service`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service: Option<ManagedResourceDefinitionConversionWebhookClientConfigService>,
    /// url gives the location of the webhook, in standard URL form
    /// (`scheme://host:port/path`). Exactly one of `url` or `service`
    /// must be specified.
    /// 
    /// The `host` should not refer to a service running in the cluster; use
    /// the `service` field instead. The host might be resolved via external
    /// DNS in some apiservers (e.g., `kube-apiserver` cannot resolve
    /// in-cluster DNS as that would be a layering violation). `host` may
    /// also be an IP address.
    /// 
    /// Please note that using `localhost` or `127.0.0.1` as a `host` is
    /// risky unless you take great care to run this webhook on all hosts
    /// which run an apiserver which might need to make calls to this
    /// webhook. Such installs are likely to be non-portable, i.e., not easy
    /// to turn up in a new cluster.
    /// 
    /// The scheme must be "https"; the URL must begin with "<https://".>
    /// 
    /// A path is optional, and if present may be any string permissible in
    /// a URL. You may use the path to pass an arbitrary string to the
    /// webhook, for example, a cluster identifier.
    /// 
    /// Attempting to use a user or basic auth e.g. "user:password@" is not
    /// allowed. Fragments ("#...") and query parameters ("?...") are not
    /// allowed, either.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url: Option<String>,
}

/// service is a reference to the service for this webhook. Either
/// service or url must be specified.
/// 
/// If the webhook is running within the cluster, then you should use `service`.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct ManagedResourceDefinitionConversionWebhookClientConfigService {
    /// name is the name of the service.
    /// Required
    pub name: String,
    /// namespace is the namespace of the service.
    /// Required
    pub namespace: String,
    /// path is an optional URL path at which the webhook will be contacted.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub path: Option<String>,
    /// port is an optional service port at which the webhook will be contacted.
    /// `port` should be a valid port number (1-65535, inclusive).
    /// Defaults to 443 for backward compatibility.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub port: Option<i32>,
}

/// Names specify the resource and kind names for the custom resource.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct ManagedResourceDefinitionNames {
    /// categories is a list of grouped resources this custom resource belongs to (e.g. 'all').
    /// This is published in API discovery documents, and used by clients to support invocations like
    /// `kubectl get all`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub categories: Option<Vec<String>>,
    /// kind is the serialized kind of the resource. It is normally CamelCase and singular.
    /// Custom resource instances will use this value as the `kind` attribute in API calls.
    pub kind: String,
    /// listKind is the serialized kind of the list for this resource. Defaults to "`kind`List".
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "listKind")]
    pub list_kind: Option<String>,
    /// plural is the plural name of the resource to serve.
    /// The custom resources are served under `/apis/<group>/<version>/.../<plural>`.
    /// Must match the name of the CustomResourceDefinition (in the form `<names.plural>.<group>`).
    /// Must be all lowercase.
    pub plural: String,
    /// shortNames are short names for the resource, exposed in API discovery documents,
    /// and used by clients to support invocations like `kubectl get <shortname>`.
    /// It must be all lowercase.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "shortNames")]
    pub short_names: Option<Vec<String>>,
    /// singular is the singular name of the resource. It must be all lowercase. Defaults to lowercased `kind`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub singular: Option<String>,
}

/// ManagedResourceDefinitionSpec specifies the desired state of the resource definition.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum ManagedResourceDefinitionScope {
    Namespaced,
    Cluster,
}

/// ManagedResourceDefinitionSpec specifies the desired state of the resource definition.
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]
pub enum ManagedResourceDefinitionState {
    Active,
    Inactive,
}

/// CustomResourceDefinitionVersion describes a version for CRD.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct ManagedResourceDefinitionVersions {
    /// AdditionalPrinterColumns specifies additional columns returned in Table output.
    /// See <https://kubernetes.io/docs/reference/using-api/api-concepts/#receiving-resources-as-tables> for details.
    /// If no columns are specified, a single column displaying the age of the custom resource is used.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "additionalPrinterColumns")]
    pub additional_printer_columns: Option<Vec<ManagedResourceDefinitionVersionsAdditionalPrinterColumns>>,
    /// Deprecated indicates this version of the custom resource API is deprecated.
    /// When set to true, API requests to this version receive a warning header in the server response.
    /// Defaults to false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deprecated: Option<bool>,
    /// DeprecationWarning overrides the default warning returned to API clients.
    /// May only be set when `deprecated` is true.
    /// The default warning indicates this version is deprecated and recommends use
    /// of the newest served version of equal or greater stability, if one exists.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "deprecationWarning")]
    pub deprecation_warning: Option<String>,
    /// Name is the version name, e.g. “v1”, “v2beta1”, etc.
    /// The custom resources are served under this version at `/apis/<group>/<version>/...` if `served` is true.
    pub name: String,
    /// Schema describes the schema used for validation, pruning, and defaulting of this version of the custom resource.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema: Option<ManagedResourceDefinitionVersionsSchema>,
    /// SelectableFields specifies paths to fields that may be used as field selectors.
    /// A maximum of 8 selectable fields are allowed.
    /// See <https://kubernetes.io/docs/concepts/overview/working-with-objects/field-selectors>
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "selectableFields")]
    pub selectable_fields: Option<Vec<ManagedResourceDefinitionVersionsSelectableFields>>,
    /// Served is a flag enabling/disabling this version from being served via REST APIs
    pub served: bool,
    /// Storage indicates this version should be used when persisting custom resources to storage.
    /// There must be exactly one version with storage=true.
    pub storage: bool,
    /// Subresources specify what subresources this version of the defined custom resource have.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub subresources: Option<ManagedResourceDefinitionVersionsSubresources>,
}

/// CustomResourceColumnDefinition specifies a column for server side printing.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct ManagedResourceDefinitionVersionsAdditionalPrinterColumns {
    /// description is a human readable description of this column.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// format is an optional OpenAPI type definition for this column. The 'name' format is applied
    /// to the primary identifier column to assist in clients identifying column is the resource name.
    /// See <https://github.com/OAI/OpenAPI-Specification/blob/master/versions/2.0.md#data-types> for details.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub format: Option<String>,
    /// jsonPath is a simple JSON path (i.e. with array notation) which is evaluated against
    /// each custom resource to produce the value for this column.
    #[serde(rename = "jsonPath")]
    pub json_path: String,
    /// name is a human readable name for the column.
    pub name: String,
    /// priority is an integer defining the relative importance of this column compared to others. Lower
    /// numbers are considered higher priority. Columns that may be omitted in limited space scenarios
    /// should be given a priority greater than 0.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub priority: Option<i32>,
    /// type is an OpenAPI type definition for this column.
    /// See <https://github.com/OAI/OpenAPI-Specification/blob/master/versions/2.0.md#data-types> for details.
    #[serde(rename = "type")]
    pub r#type: String,
}

/// Schema describes the schema used for validation, pruning, and defaulting of this version of the custom resource.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct ManagedResourceDefinitionVersionsSchema {
    /// OpenAPIV3Schema is the OpenAPI v3 schema to use for validation and
    /// pruning.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "openAPIV3Schema")]
    pub open_apiv3_schema: Option<BTreeMap<String, serde_json::Value>>,
}

/// SelectableField specifies the JSON path of a field that may be used with field selectors.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct ManagedResourceDefinitionVersionsSelectableFields {
    /// jsonPath is a simple JSON path which is evaluated against each custom resource to produce a
    /// field selector value.
    /// Only JSON paths without the array notation are allowed.
    /// Must point to a field of type string, boolean or integer. Types with enum values
    /// and strings with formats are allowed.
    /// If jsonPath refers to absent field in a resource, the jsonPath evaluates to an empty string.
    /// Must not point to metdata fields.
    /// Required.
    #[serde(rename = "jsonPath")]
    pub json_path: String,
}

/// Subresources specify what subresources this version of the defined custom resource have.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct ManagedResourceDefinitionVersionsSubresources {
    /// scale indicates the custom resource should serve a `/scale` subresource that returns an `autoscaling/v1` Scale object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scale: Option<ManagedResourceDefinitionVersionsSubresourcesScale>,
    /// status indicates the custom resource should serve a `/status` subresource.
    /// When enabled:
    /// 1. requests to the custom resource primary endpoint ignore changes to the `status` stanza of the object.
    /// 2. requests to the custom resource `/status` subresource ignore changes to anything other than the `status` stanza of the object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<ManagedResourceDefinitionVersionsSubresourcesStatus>,
}

/// scale indicates the custom resource should serve a `/scale` subresource that returns an `autoscaling/v1` Scale object.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct ManagedResourceDefinitionVersionsSubresourcesScale {
    /// labelSelectorPath defines the JSON path inside of a custom resource that corresponds to Scale `status.selector`.
    /// Only JSON paths without the array notation are allowed.
    /// Must be a JSON Path under `.status` or `.spec`.
    /// Must be set to work with HorizontalPodAutoscaler.
    /// The field pointed by this JSON path must be a string field (not a complex selector struct)
    /// which contains a serialized label selector in string form.
    /// More info: <https://kubernetes.io/docs/tasks/access-kubernetes-api/custom-resources/custom-resource-definitions#scale-subresource>
    /// If there is no value under the given path in the custom resource, the `status.selector` value in the `/scale`
    /// subresource will default to the empty string.
    #[serde(default, skip_serializing_if = "Option::is_none", rename = "labelSelectorPath")]
    pub label_selector_path: Option<String>,
    /// specReplicasPath defines the JSON path inside of a custom resource that corresponds to Scale `spec.replicas`.
    /// Only JSON paths without the array notation are allowed.
    /// Must be a JSON Path under `.spec`.
    /// If there is no value under the given path in the custom resource, the `/scale` subresource will return an error on GET.
    #[serde(rename = "specReplicasPath")]
    pub spec_replicas_path: String,
    /// statusReplicasPath defines the JSON path inside of a custom resource that corresponds to Scale `status.replicas`.
    /// Only JSON paths without the array notation are allowed.
    /// Must be a JSON Path under `.status`.
    /// If there is no value under the given path in the custom resource, the `status.replicas` value in the `/scale` subresource
    /// will default to 0.
    #[serde(rename = "statusReplicasPath")]
    pub status_replicas_path: String,
}

/// status indicates the custom resource should serve a `/status` subresource.
/// When enabled:
/// 1. requests to the custom resource primary endpoint ignore changes to the `status` stanza of the object.
/// 2. requests to the custom resource `/status` subresource ignore changes to anything other than the `status` stanza of the object.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct ManagedResourceDefinitionVersionsSubresourcesStatus {
}

/// ManagedResourceDefinitionStatus shows the observed state of the resource definition.
#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq)]
pub struct ManagedResourceDefinitionStatus {
    /// Conditions of the resource.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub conditions: Option<Vec<Condition>>,
}

