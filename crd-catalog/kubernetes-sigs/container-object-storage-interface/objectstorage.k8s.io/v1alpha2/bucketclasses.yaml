apiVersion: "apiextensions.k8s.io/v1"
kind: "CustomResourceDefinition"
metadata:
  annotations:
    api-approved.kubernetes.io: "unapproved, experimental v1alpha2 changes"
    controller-gen.kubebuilder.io/version: "v0.19.0"
  name: "bucketclasses.objectstorage.k8s.io"
spec:
  group: "objectstorage.k8s.io"
  names:
    kind: "BucketClass"
    listKind: "BucketClassList"
    plural: "bucketclasses"
    singular: "bucketclass"
  scope: "Cluster"
  versions:
    - name: "v1alpha2"
      schema:
        openAPIV3Schema:
          description: "BucketClass defines a named \"class\" of object storage buckets.\nDifferent classes might map to different object storage protocols, quality-of-service levels,\nbackup policies, or any other arbitrary configuration determined by storage administrators.\nThe name of a BucketClass object is significant, and is how users can request a particular class."
          properties:
            apiVersion:
              description: "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
              type: "string"
            kind:
              description: "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
              type: "string"
            metadata:
              type: "object"
            spec:
              description: "spec defines the BucketClass. spec is entirely immutable."
              properties:
                deletionPolicy:
                  description: "deletionPolicy determines whether a Bucket created through the BucketClass should be deleted\nwhen its bound BucketClaim is deleted.\nPossible values:\n - Retain: keep both the Bucket object and the backend bucket\n - Delete: delete both the Bucket object and the backend bucket"
                  enum:
                    - "Retain"
                    - "Delete"
                  type: "string"
                driverName:
                  description: "driverName is the name of the driver that fulfills requests for this BucketClass."
                  minLength: 1
                  type: "string"
                parameters:
                  additionalProperties:
                    type: "string"
                  description: "parameters is an opaque map of driver-specific configuration items passed to the driver that\nfulfills requests for this BucketClass."
                  type: "object"
              required:
                - "deletionPolicy"
                - "driverName"
              type: "object"
              x-kubernetes-validations:
                - message: "BucketClass spec is immutable"
                  rule: "self == oldSelf"
          required:
            - "spec"
          type: "object"
      served: true
      storage: true
